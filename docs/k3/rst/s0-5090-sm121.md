# RST session — S0/S1 5090 SM121 launch

CHARTER
-----------------------------------------------
Find whether Atlas GB10 SM121 images can run on the 5090 (SM120), or whether the 5090 is PyTorch/shape-debug only.

#AREAS
S0-5090
platform-cuda
sm121-compat

START
2026-09-11

TESTER
umbrella / lab

ORACLE
Device attribute `sm_120`. Launch error string from CUDA runtime.
Known-bad / control: the same source compiled `-arch=sm_120` must launch. If the control fails, the instrument is broken and an SM121 fail is meaningless.

KNOWN-BAD (control)
`nvcc -arch=sm_120` → `launch: no error` / `sync: no error`. Instrument works.

TEST NOTES
- nvcc 13.0.88 on train WSL.
- Trivial `__global__ void k()`.
- `nvcc -arch=sm_121` cubin: `launch: no kernel image is available for execution on the device`.
- `nvcc -gencode=arch=compute_121,code=compute_121` PTX: same `no kernel image` (no JIT down to sm_120).
- Consequence: 5090 is not a GPU-milestone host for this PRD.

BUGS
#N/A (expected incompatibility, recorded as a platform fact)

STOP
Charter complete. No more news available without a different GPU.
