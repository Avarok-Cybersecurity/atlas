{
  "summary": "Map the MoE mgemm serving arm and GDN/attention native routing for EXL3",
  "agentCount": 4,
  "logs": [],
  "result": {
    "read:upstream-moe": {
      "summary": "Upstream ExLlamaV3 serves EXL3 MoE through THREE distinct tiers keyed on token count T (rows after flattening bsz*q_len), never one mechanism: (1) T<=MAX_BSZN=8 -> BC_BlockSparseMLP.run_bszN, a per-T CUDA graph doing exactly three exl3_mgemm calls (gate, up, down) over bszm = T*top_k slots (qwen4_exp: 10T<=80 slots), A gathered so slot s=t*top_k+j holds a copy of token t's row, indices/weights passed as a flat (1, bszm) view, num_tokens=T, weighted per-token-group reduction landing in C[0..T-1]; the shared expert + sigmoid shared-gate are fused into the same graph on the ungathered (1,T,H) view. (2) T>8 (and T>=f_threshold=min(num_experts//top_k,4)=4) -> sort-by-expert path: argsort of local expert ids + bincount, then the dedicated fused persistent kernel ext.exl3_moe handles every expert with 0<count<=TEMP_ROWS_FUSED=128 rows in ONE launch (no mgemm at all), with a no-host-sync shortcut when T*top_k<=128; experts exceeding 128 rows fall back to per-expert dequantize-to-fp16 + hgemm (run_single_expert_dq), i.e. reconstruction, not trellis math. The mgemm 128-slot model is NEVER used for prefill. (3) The shared expert is a plain GatedMLP, never a routed slot: bsz<=8 fused in BC graph, 9..32 via a 2-wide gate+up mgemm pointer table, >32 separate linears (>144 rows -> reconstruct+hgemm). EP shards keep GLOBAL expert ids in indices and pass min_index/max_index; remote slots are masked -1 in place (num_tokens>1) or compacted (num_tokens==1), producing partial sums summed by all_reduce. Everything is fp16 activations with fp32 MoE output.",
      "contracts": [
        {
          "name": "Q1 DECODE: three mgemm calls over T*top_k slots, one CUDA graph per T (1..8)",
          "file_line": "upstream exllamav3_ext/libtorch/blocksparse_mlp.cpp:67-225 (fetched to /tmp/bsmlp.cpp), block_sparse_mlp.py:1229-1243 + python mirror 1331-1398 (/tmp/block_sparse_mlp.py)",
          "detail": "Decode (T = bsz*q_len <= MAX_BSZN = 8) runs BC_BlockSparseMLP.run_bszN: numex = top_k = 10, bszm = T*numex (<= 80). num_tokens==1: A = y.unsqueeze(0) = (1,1,k) zero-copy broadcast (kernel's bszm_in==1 fast path transforms A[0] once per selected expert since suh is per-matrix). num_tokens>1: A is a GATHERED (bszm,1,Hi) buffer a_gather built eagerly (not graphed) via index_select with flat_token = arange(T).unsqueeze(1).expand(T,numex).reshape(-1) -- slot s = t*top_k+j holds a COPY of token t's row (bsmlp.cpp:242-284). Then exactly three exl3_mgemm calls, each with sel_idx = selected_experts.reshape(1,-1) [(1,bszm) int64 view], num_tokens=T: GATE: A=yi, B=gate ptrs, C=interm_g[(bszm,1,640) fp16], A_had=yh[(bszm,1,2560)], weights=None. UP: same with interm_u. ACTIVATION between up and down, on the slot buffers, via ext.silu_mul(interm_g, interm_u, interm_a, act_limit) -- gate/up are NOT fused into a 2x pointer table in the MoE path (that trick is GatedMLP-only). DOWN: A=interm_a[(bszm,1,640)], C=out_d[(bszm,1,2560) fp32], A_had=interm_g (reused; 'A_had must not alias A: the autotuner relaunches on the first call'), weights=w_idx=(1,bszm) fp16 -> kernel scales each slot result by weights[s] and reduces each token's group of top_k consecutive slots; 'rows 0..num_tokens-1 hold the weighted reductions' (bsmlp.cpp:203-205), python reads out_d[:T].view(x.shape) (block_sparse_mlp.py:1242). bszm_in/bszm_out are capped by num_indices = indices.size(1) in the host wrapper (exl3_gemm.cu:461-468 local ref) -- hence the flat (1,bszm) view is mandatory; a (T,top_k) shape would cap bszm at top_k. No sort/dedup at this tier ('overlap between tokens this small is rare, not worth the argsort/bincount host-sync', block_sparse_mlp.py:1229-1236). Captured as one CUDA graph per T from the 3rd invocation, replayed with pointer patching (GP_mgemm_A/C/indices/weights)."
        },
        {
          "name": "Q2 PREFILL: sort-by-expert + fused exl3_moe kernel + per-expert dequant overflow -- mgemm NOT used",
          "file_line": "/tmp/block_sparse_mlp.py:1054-1227 (forward torch/fused path), /tmp/exl3_moe.cu:36-301, TEMP_ROWS_FUSED=128 TEMP_ROWS_GRAPH=32 at block_sparse_mlp.py:19-20",
          "detail": "For T > MAX_BSZN(8): flat_expert_global = selected_experts.reshape(-1) [T*top_k int64], flat_weight likewise fp16, flat_token = interleaved arange; local-id map (global - routing_first, invalid -> sentinel E); order = flat_expert_local.argsort(); token_sorted/weight_sorted = flat_token[order]/flat_weight[order]; expert_count = bincount(minlength=E+1). Fast path: if T*top_k <= TEMP_ROWS_FUSED=128 (qwen4_exp: T<=12) call ext.exl3_moe once with num_active=-1 and NO expert_count.tolist() host sync (added because that sync was ~33% idle at MTP verify shapes, block_sparse_mlp.py:1138-1144). Otherwise: expert_count.tolist() (one CPU sync per layer), ext.exl3_moe processes every expert with 0 < count <= 128 rows in one persistent cooperative launch (grid dim3(group_size,1,num_groups), num_groups = min(num_sms/MOE_SMS_PER_EXPERT, MOE_MAX_GROUPS, num_active), group widened to MOE_MAX_SMS_PER_EXPERT=32 when few active; MOE_SMS_PER_EXPERT=8, MOE_TILESIZE_K=32, block_dim=256*32/16=512), then a python loop over experts with count > 128 runs bc.run_single_expert_dq: dequantize gate/up/down to fp16 (dq_temp (Hi,I) reused as (I,Ho)) + hgemm on the count gathered rows, then current_state.mul_(w); final.index_add_(0, top_x, ...). If exl3_moe unsupported, min_rows=0 and the loop uses bc.run_single_expert (graphed quantized path) for count<=TEMP_ROWS_GRAPH=32, DQ above 32. CRITICAL eligibility: exl3_moe REQUIRES a uniform mcg-or-mul1 codebook across gate/up/down -- 'Only mcg and mul1 codebooks are supported' (exl3_moe.cu:182-185; support_fused check block_sparse_mlp.py:696-699); plain-codebook (cb=0) checkpoints get NO fused prefill kernel and run graph/DQ per expert. exl3_moe args: hidden_state (T,H) fp16; output_state (T,H) fp32 ZERO-INITIALIZED (kernel accumulates weighted index_add); expert_count (E+1,) int64 (last entry = sentinel bucket, ignored -- 'for the case where some tokens activate fewer experts, specifically in expert split mode'); token_sorted/weight_sorted (T*top_k,) int64/fp16; temp_state_g/u (C, 128, H) fp16 and temp_intermediate_g/u (C, 128, I) fp16 where C = num_sms/8 = exl3_moe_max_concurrency; per-projection ptr tables (E,) + separate K_gate/K_up/K_down; kernel index [4*K + 2*cb + (dims%256==0)]."
        },
        {
          "name": "Q3 SHARED EXPERT: plain GatedMLP module, never an mgemm slot; sigmoid shared gate",
          "file_line": "/tmp/block_sparse_mlp.py:769-783,1423-1436; /tmp/bsmlp.cpp:210-224; local ref py_mlp.py:695-766 (GatedMLP.forward), py_arch_qwen4_exp.py:201,207-220",
          "detail": "qwen4_exp wires shared_experts = GatedMLP(intermediate=shared_expert_intermediate_size, silu, interm fp16, out fp32) and key_shared_gate = 'shared_expert_gate' (Linear hidden->1, fp16 weights). It is NEVER in the routed pointer tables. T<=8: fused into the BC graph (bc_sh_exp: requires GatedMLP.bc built and no post-norm) -- run on the UNGATHERED x_dense (1,T,Hi) view, then add_sigmoid_gate_proj(shared_out, x, routed_out, shared_gate.weight): final += sigmoid(x @ w_sg) * shared_out (bsmlp.cpp:210-224). T>8: shared_experts.forward(x) after the routed sum: rows<=MAX_BSZN(8) its own BC multi-row graph; rows<=32 one fused gate+up exl3_mgemm with a 2-entry pointer table (bszm=2, A=(1,m,dim) broadcast, C=gu(2,m,out_f), A_had=guh(2,m,dim), indices=None weights=None num_tokens=1; py_mlp.py:731-757) then silu_mul + down Linear; rows>32 three separate Linear.forwards (each switching to reconstruct+hgemm above AUTO_RECONSTRUCT_THRESHOLD=144 rows). Shared gate application at T>32: z = shared_gate.forward(x); ext.add_sigmoid_gate(y, z, final); at T<=32: ext.add_sigmoid_gate_proj(y, x, final, weight) (block_sparse_mlp.py:1429-1434)."
        },
        {
          "name": "Q4 EP / MULTI-GPU: global ids + min/max_index filtering; remote tokens masked to zero, all_reduce sums partials",
          "file_line": "/tmp/block_sparse_mlp.py:737-746,1034-1037,1086-1099,1588-1595; local ref exl3_gemm.cu:371-377,412-414",
          "detail": "EP import (unit='experts') keeps only local Linear lists and sets routing_first/routing_last (global interval); min_expert/max_expert = (routing_first, routing_last), or (-1,-1) when the module holds all experts (block_sparse_mlp.py:737-746). Routing runs ONLY on routing_device (the rank holding routing_gate) and selected_experts/routing_weights are broadcast to all ranks (1035-1037). indices passed to mgemm stay GLOBAL: with min_index>=0 the kernel drops selections outside [min_index, max_index) and rebases retained q by min_index so B/suh/svh are shard-local tables. num_tokens==1: retained indices+weights are COMPACTED; num_tokens>1: out-of-range slots masked to -1 IN PLACE, preserving the fixed per-token slot runs the grouped reduction depends on and the slot->input-row correspondence (exl3_gemm.cu doc 371-377 and 412-414). Remote-expert tokens thus contribute exact zeros to the local partial sum; completion comes from params['backend'].all_reduce(final_hidden_states) (tp_reduce, before any nonlinear post-norm). Sorted prefill path under EP: flat_expert_local = global - routing_first, invalid -> sentinel E sorted to the tail bucket; bincount minlength E+1; expert loop covers only 0..E-1; exl3_moe's expert_count last entry is that sentinel bucket and is ignored. Hard cap: range filtering supports at most 128 slots (kernel index-compaction capacity, exl3_gemm.cu:382-383) -- with top_k=10 that binds the mgemm tier to T<=12, already satisfied by MAX_BSZN=8."
        },
        {
          "name": "Q5 A_had SLABS: always m=1 per slot in the MoE mgemm tier; sized bszm*m*k, buffers cached and reused",
          "file_line": "/tmp/block_sparse_mlp.py:711-735; local ref exl3_gemm.cu:453-456; py_mlp.py:655-660,733",
          "detail": "The MoE mgemm tier NEVER passes large m: every slot is m=1 (A is (bszm,1,k)), bszm <= MAX_BSZN*top_k = 80. A_had slabs: gate/up use yh = temp_hidden[:bszn_rows].view(80,1,2560) (80*2560 halves = 400KB); down reuses interm_g (80,1,640) as its A_had after the activation frees it. Host check: A_had.numel() >= bszm*m*k, 'an undersized scratch is silent OOB corruption (found the hard way)' (exl3_gemm.cu:453-456). The only m>1 mgemm anywhere is GatedMLP fused gate+up at m = rows <= 32, bszm = 2: guh = (2, m, hidden) fp16, i.e. 2*32*2560 halves max. All slot buffers come from g_tensor_cache (exact-shape-keyed, never evicts, one entry per name) sized max(TEMP_ROWS_GRAPH*2, bszn_rows) rows and shared between the graph/dq/fused-bsz1 paths (block_sparse_mlp.py:711-735); prefill-scale inputs never reach mgemm, so slabs never scale with T."
        },
        {
          "name": "Q6 MTP HEAD: identical BlockSparseMLP path; the T*top_k<=128 no-sync exl3_moe branch exists FOR MTP verify",
          "file_line": "local ref py_arch_qwen4_exp.py:86-104 (build_qwen4_block shared by trunk and MTP), py_qwen4_exp_mtp.py:1-163; /tmp/block_sparse_mlp.py:1138-1144",
          "detail": "Qwen4ExpMTPModel builds its decoder blocks through the same build_qwen4_block factory (py_arch_qwen4_exp.py comment: 'One qwen4_exp decoder block (shared by the trunk and the MTP head)'), so MoE serving is bit-identical to the trunk; py_qwen4_exp_mtp.py only defines the MTP input layer (pre_fc_norm_hidden grouped-norm over the hc_mult*hidden stream tap + fc_hidden/fc_embedding combine). The only MoE code motivated by MTP is the prefill fast branch: at verify shapes (gamma draft tokens, T*top_k <= TEMP_ROWS_FUSED=128, i.e. T<=12 at top-10) exl3_moe runs with num_active=-1 and skips the expert_count.tolist() host sync, which had cost '~33% idle at MTP verify shapes' (block_sparse_mlp.py:1141). T<=8 verify still prefers the run_bszN graph tier."
        },
        {
          "name": "Q7 ROUTING TENSOR SEMANTICS Atlas must reproduce",
          "file_line": "/tmp/bsmlp.cpp:15-64,109-113; /tmp/block_sparse_mlp.py:64-105,899-919; local ref py_arch_qwen4_exp.py:188-206",
          "detail": "qwen4_exp uses router_type='std' (arch passes none; no e_score bias, no scaling factor, no groups). Before mgemm: selected_experts (bsz, top_k) torch.long (int64) holding GLOBAL expert ids in [0,512); routing_weights (bsz, top_k) torch.half = softmax over the top-k router LOGITS (mathematically the full softmax renormalized over the selected set; bsmlp.cpp:36-48: matmul_out -> topk_out(k=10, largest, unsorted-ok) -> softmax_out over the k logits). bsz==1 writes into preallocated statics router_logits_bsz1 (1,512) fp16 / selected_experts_bsz1 (1,10) int64 / routing_weights_bsz1 (1,10) fp16 whose addresses are baked into the CUDA graph. router logits computed in fp16 (y @ gate_tensor fp16, gate stored untransposed with a lazily-built transposed copy for the bsz-1 GEMV kernel). For the mgemm calls both tensors are reshaped to (1, bsz*top_k) contiguous row-major -- slot s = t*top_k + j maps to indices[0,s] = selected_experts[t,j], weights[0,s] = routing_weights[t,j]; num_indices = size(1) = bszm; negative index = inactive slot. For exl3_moe both are flattened to (T*top_k,) then permuted by argsort(local_expert_id); weight dtype stays fp16 end-to-end and the down-projection reduction accumulates in fp32 C."
        }
      ],
      "design": "Recommended Atlas reproduction for serving 512 routed experts + shared expert directly from packed trellis (all consistent with the already-parity-proven mgemm in kernels/gb10/common/exl3_matmul.cu, whose num_tokens/per-token-reduction/min-max filtering contract matches upstream exactly):\n\n1. DECODE TIER (spec-verify included, T <= 8, upstream MAX_BSZN): implement a BlockSparseExl3 layer in crates/spark-model/src/layers/ that per forward (a) computes routing (existing qwen4_exp router; produce ids i64 [T,10] GLOBAL, probs f16 [T,10] renormalized-softmax); (b) for T==1 passes A=(1,1,2560) broadcast, for T=2..8 gathers A into a persistent (80,1,2560) slab so slot s=t*10+j copies token t's row; (c) issues exactly three mgemm launches -- gate (C=interm_g f16 (80,1,640), A_had=yh (80,1,2560)), up (interm_u), silu_mul on the slot buffers, down (A=interm_a, A_had=interm_g reused NEVER aliasing A, weights=probs flat (1,80), C fp32 (80,1,2560), num_tokens=T) -- and reads C rows 0..T-1. Keep indices/weights as FLAT (1, T*10) views (the host caps bszm by indices.size(1)). Persist all six slot buffers per layer (or one arena-shared set: upstream shares one g_tensor_cache entry per name across all 48 layers -- do the same via BufferArena to avoid 48x duplication).\n\n2. PREFILL TIER (T > 8): do NOT extend mgemm slots. Port the sort-by-expert strategy: flatten ids/probs, map to local ids with sentinel E, argsort + bincount(E+1). Two sub-paths: (a) port the exl3_moe persistent fused kernel (grid (group_size,1,num_groups), MOE_SMS_PER_EXPERT=8 -> GB10 concurrency C=num_sms/8, temps (C,128,2560) f16 x2 + (C,128,640) f16 x2, expert row cap 128, ONLY for mcg/mul1 codebooks -- check what codebook the target 2.05bpw checkpoint uses FIRST; if plain cb=0, this kernel cannot serve it and upstream itself would fall through); (b) overflow experts (count>128) via per-expert reconstruct-to-bf16 + existing GEMM -- Atlas already has NVFP4 materialization machinery; a transient per-expert dequant (2560x640 f16 = 3.3MB scratch) + cuBLAS/CUTLASS bf16 GEMM mirrors upstream run_single_expert_dq and avoids holding any dense copy. Add upstream's no-sync shortcut: if T*10 <= 128 skip the bincount readback entirely (MTP verify hot path; cf. small-M MoE prefill trap memory note -- this is the same lesson upstream learned).\n\n3. SHARED EXPERT: keep it a dense GatedMLP served by the existing exl3 gemm/gemv wrappers (never in the routed tables); apply shared_expert_gate as final += sigmoid(x @ w_sg) * shared_out. Optional decode optimization: 2-slot gate+up mgemm pointer table exactly like upstream GatedMLP (bszm=2, A broadcast, A_had (2,m,2560)).\n\n4. EP=2: build per-rank pointer tables over local experts only; pass GLOBAL ids with min_index/max_index = the rank's [first, last); rely on the kernel's mask-in-place (num_tokens>1) / compaction (num_tokens==1) and all-reduce the fp32 partial. In the sorted prefill tier use the sentinel-bucket scheme so remote assignments sort to the tail and are skipped. Note 128-slot filtering cap => mgemm tier hard-limited to T<=12 at top-10; keep the tier boundary at 8.\n\n5. FILE TOUCHES: kernels/gb10/common/exl3_matmul.cu (add exl3_moe port only if checkpoint codebook qualifies), crates/spark-model/src/layers/ops/exl3_matmul*.rs (bszN slot-buffer plumbing + gather kernel), new crates/spark-model/src/layers/moe_exl3.rs (three-call composition + sort path), crates/spark-model/src/weight_map/exl3_materialize.rs (stop materializing to NVFP4; keep trellis + build per-projection i64 pointer tables, ~30GB vs ~59GB), wire behind ATLAS_EXL3_NATIVE=1 next to lm_head_exl3.rs. Numerics: upstream is fp16-activation end-to-end with fp32 down-C; Atlas is BF16 -- keep the mgemm A in fp16 (convert at the layer boundary, as the parity harness already does) and accumulate the routed sum in fp32 before converting back.",
      "risks": [
        "Version provenance: block_sparse_mlp.py, blocksparse_mlp.cpp, multilinear.py and exl3_moe.cu were fetched from upstream turboderp-org/exllamav3 master (2026-09-01) because the local .research/exllamav3_ref snapshot omits them; the local snapshot's qwen4_exp branch may differ in detail (the local py_mlp.py matches this master's GatedMLP byte-for-byte in the mgemm call, and py_arch_qwen4_exp.py's BlockSparseMLP kwargs all exist in this ctor, so drift risk is low but nonzero).",
        "exl3_moe fused prefill kernel refuses plain-codebook (cb=0) weights -- TORCH_CHECK 'Only mcg and mul1 codebooks are supported'. If the target 2.05bpw checkpoint quantized with the plain codebook, upstream itself has NO fused prefill path and serves large T via per-expert dequant+hgemm; verify the checkpoint's mcg/mul1 tensors before budgeting an exl3_moe port.",
        "Prefill throughput cliff: at top-10/512 experts, expected rows per expert is ~T/51; beyond T~6500 the average expert exceeds the 128-row fused cap and the path degrades to per-expert dequant + fp16 GEMM (memory-bound reconstruct per expert per layer). This is upstream's actual behavior, not a gap in the extraction -- Atlas's chunked-prefill (2048/4096 chunks per qwen4exp canonical config) keeps chunks below the cliff.",
        "The bszm cap by indices.size(1) (exl3_gemm.cu:461-468) makes the flat (1, bszm) indices view load-bearing; passing (T, top_k) silently truncates bszm to top_k and produces wrong-but-plausible outputs.",
        "A_had aliasing: upstream explicitly reuses interm_g as the down-projection's A_had but warns A_had must NOT alias A because the autotuner relaunches the kernel on the first call; Atlas's autotune-equivalent (if any) must preserve this or dedicate a separate slab.",
        "expert_count host readback is a per-layer CPU sync (~33% idle at MTP verify shapes upstream); Atlas must implement the T*top_k<=128 no-sync branch or spec-decode verify performance will regress (same failure mode as the memory note 'small-M MoE prefill trap').",
        "Routing numerics: upstream computes router logits in fp16 and weights as softmax over top-k logits; Atlas's existing router may softmax the full distribution then renormalize -- mathematically identical only if done over the same logits; a bf16-vs-fp16 logit difference can flip near-tied expert selections (cf. MTP greedy-divergence sensitivity).",
        "num_experts_per_tok upper bound: upstream asserts top_k <= TEMP_ROWS_GRAPH=32 and MAX_BSZN*top_k slot buffers; qwen4_exp's 10 fits, but the 128-slot filtered-mgemm cap means EP + batched verify beyond T=12 must not route through the mgemm tier."
      ]
    },
    "read:atlas-moe": {
      "summary": "Atlas serves qwen4_exp's 512 routed experts through four decode surfaces (C=1 fused GEMV, K=2/K=3 fused batch, token-major N>=4, per-token batched fallback) and one grouped-GEMM prefill ladder, all driven by device-resident routing state at ctx.buffers.scratch() (u32 indices + f32 probs) and per-expert pointer tables (ExpertPtrTable: [E] u64 packed_ptrs / [E] u64 scale_ptrs / [E] f32 scale2). The native EXL3 mgemm form maps cleanly: its b_indices IS Atlas's indices_dev, its b_weights grouped-reduction IS moe_weighted_sum_blend's routed half, and its min/max_index filtering IS EP's local_expert_range. The natural plug-in is a fourth expert-format arm (alongside NVFP4/FP8/BF16) keyed by an Exl3ExpertPtrTable {trellis_list, suh_list, svh_list} built in ptr_table_build.rs, dispatched in moe/forward.rs (decode), a token-major-style batched arm for n=2..8, and a per-expert-loop or <=128-slot mgemm-batch arm inside forward_prefill_routed.rs reusing moe_sort_by_expert / expert_offsets / sorted_token_ids / token_to_perm / moe_unpermute_reduce_indexed unchanged. Hard constraints: cooperative launches veto CUDA-graph decode capture (the exl3 lm_head already wires this veto at both decode decision points), the mgemm filtered path caps bszm at 128 and uses module-scope __device__ globals (serialize per device), one launch serves ONE k_bits (mixed-K checkpoints need per-K grouping), and A_had/C/locks slabs must be pre-allocated at construction (901 playbook: no hot-path allocs).",
      "contracts": [
        {
          "name": "DECODE C=1 routed-expert path (the main serving path)",
          "file_line": "crates/spark-model/src/layers/moe/forward.rs:60-754",
          "detail": "MoeLayer::forward(input,[1,H] BF16). Router: w4a16_decode_gemv on gate_nvfp4 OR dense_gemv on weights.gate -> gate_logits (forward.rs:149-177); qwen4_exp takes the plain softmax arm ops::moe_topk_softmax (forward.rs:275-286, no correction bias/hash). Routing state: indices_dev = ctx.buffers.scratch()+0 as u32[top_k=10], weights_dev = scratch+top_k*4 as f32[top_k] (forward.rs:139-141); scratch region sized 2*m*top_k*4 bytes in buffers/sizes.rs:179. Experts: NVFP4 branch (forward.rs:500-604) = 2 fused kernels: ops::moe_expert_gate_up_shared (grid [ceil(inter/8), top_k+1, 2], moe_shared_expert_fused.cu; slot top_k = shared expert; indexes gate_ptrs/up_ptrs by indices_dev[slot]; writes slot-major expert_gate_out/expert_up_out [top_k, inter=640] BF16) then ops::moe_expert_silu_down_shared (SiLU fused INSIDE this kernel; writes expert_down_out [top_k, H=2560] BF16 + shared_out). Blend: ops::moe_weighted_sum_blend (forward.rs:685-700) does output[h] = sum_k weights_dev[k]*down_out[k,h] + sigmoid(dot(input, shared_expert_gate))*shared_out[h] + writes moe_output. EP: shared zeroed out of blend, all-reduce, then moe_batched_blend re-adds shared once (forward.rs:674-737)."
        },
        {
          "name": "DECODE n=2..8 paths (multi-seq / MTP verify)",
          "file_line": "crates/spark-model/src/layers/qwen3_ssm/trait_decode_multi_seq.rs:174-380",
          "detail": "Ladder: n=2 -> MoeLayer::forward_k2 (moe/forward_k2.rs:18, fused moe_fused_batch2 kernels, expert buffers 2*top_k slots, indices at scratch [2,top_k]); n=3 -> forward_k3; n>=4 DEFAULT -> forward_token_major_decode (moe/forward_token_major.rs:19: batched gate GEMM w4a16_gemm/dense_gemm -> moe_topk_softmax_batched (indices u32[n*top_k] at scratch, weights f32[n*top_k] at scratch+n*top_k*4) -> moe_expert_gate_up_shared_prefill / moe_expert_silu_down_shared_prefill (token-major, packs (token,slot) into grid) -> moe_weighted_sum_blend_prefill); opt-outs: ATLAS_MOE_LEGACY_PERTOKEN_DECODE=1 -> forward_batched per-token loop (moe/forward_batched.rs:12, reuses the C=1 fused kernels per token, per-token indices REUSE the same scratch slot serially), ATLAS_MOE_ATOMIC_C4_DECODE=1 (n=4), ATLAS_MOE_GROUPED_DECODE (grouped prefill path). Attention-layer FFN mirror: qwen3_attention/trait_impl/multi_seq/ffn.rs:110-135. The activation between up and down is fused into every *_silu_down_* decode kernel; the standalone ops::silu_mul (kernel handle moe_act_mul, elementwise over [rows, inter], moe_grouped_a.rs:417-434) exists only on the prefill/grouped path and is layout-agnostic — it can be reused unchanged by a native arm that emits separate gate/up outputs."
        },
        {
          "name": "PREFILL grouped ladder + sort/unpermute conventions",
          "file_line": "crates/spark-model/src/layers/moe/forward_prefill.rs:17-498 and forward_prefill_routed.rs:34-515",
          "detail": "Pipeline: router GEMM (router_gate_gemm_dense, pinned scalar kernel — do NOT reroute, forward_prefill.rs:207-221) -> moe_topk_softmax_batched (indices u32[n*top_k] at scratch, weights f32[n*top_k] at scratch+total_expanded*4, forward_prefill.rs:234-320) -> ops::moe_sort_by_expert (single 256-thread block, kernels/gb10/common/moe_permute.cu:200-247). Sort outputs ALIAS the gate_logits buffer (forward_prefill.rs:327-330): sorted_token_ids = gate_logits+0 (i32[total_expanded], value = expanded_row/topk = ORIGINAL TOKEN index for that sorted position), sorted_expert_ids = +te*4 (i32[te]), expert_offsets = +te*8 (i32[num_experts+1] prefix sum, expert e owns sorted rows [off[e], off[e+1])), token_to_perm = +te*8+(ne+1)*4 (i32[n,topk] reverse map -> sorted row). Grouped GEMMs (forward_prefill_routed.rs): moe_w4a16_fused_gate_up_k64_n128 gathers rows via sorted_token_ids IN-KERNEL from token-major expert_input and writes expert_gate_out/expert_up_out in SORTED layout [te, inter]; ops::silu_mul in place; grouped down (moe_w4a16_grouped_gemm_ptrtable_n128, sorted A so sorted_token_ids=NULL) writes expert_down_out [te, H] sorted. Unpermute: ops::moe_unpermute_reduce_indexed (moe_grouped_a2.rs:152, moe_permute.cu:95-117) does output[t,:] = sum_k weights[t,k]*expert_out[token_to_perm[t,k],:]. Grid sizing max_m_tiles: worst-case ceil(n*topk/64) or exact via D2H of expert_offsets (copy_d2h_on_stream, forward_prefill_routed.rs:90-101, disabled under graph capture). EP prefill zeroes expert_* buffers first (forward_prefill_routed.rs:134-144) because remote-expert kernels return without writing. A standalone gather kernel moe_permute_tokens (moe_grouped_a.rs:299, currently uncalled, retained) can produce expert-contiguous A rows for a per-expert-GEMM native arm. NVFP4 experts take this grouped path at ALL n (the >64-token gate applies only to BF16/FP8 branches, forward_prefill.rs:70-85); host expert offsets readback precedent: exact_tiles D2H + CUTLASS's returned cutlass_eoff Vec<i32>."
        },
        {
          "name": "MoeWeights / ExpertWeight / pointer tables — what an Exl3 variant needs",
          "file_line": "crates/spark-model/src/weight_map/moe.rs:14-45, weight_map/expert.rs:79-97, layers/moe/tables.rs:22-94, layers/moe/ptr_table_build.rs:17-135, layers/moe/init.rs:55-57",
          "detail": "MoeWeights { gate: DenseWeight [H,E] BF16, shared_expert: ExpertWeight, shared_expert_gate: DenseWeight [1], experts: Vec<ExpertWeight> (512; remote = ExpertWeight::null()) }. ExpertWeight = 3x QuantizedWeight {weight U8 [n,k/2], weight_scale FP8 [n,k/16], weight_scale_2 f32, ...}. ExpertPtrTable (tables.rs:22) = 3 device arrays built host-side by build_ptr_table (ptr_table_build.rs:50-86: flat_map ptr.to_le_bytes -> alloc E*8 -> copy_h2d). The Exl3 mirror is Exl3ExpertPtrTable { trellis_list: [E] u64, suh_list: [E] u64, svh_list: [E] u64 } per projection (gate/up/down = 9 arrays + shared k_bits/cb per table), built the same way from Vec<Exl3Weight> (spark_runtime::weights::exl3::Exl3Weight { trellis, suh, svh, in_dim, out_dim, k_bits, cb }) — these lists are exactly mgemm's b_list/suh_list/svh_list, indexed by B_indices values (= expert ids), so the table must be [num_experts]-wide with remote entries null (never dereferenced because min/max_index filtering skips them). Loader: weight_map/ssm_qwen35.rs:89-301 load_moe_qwen35 builds experts via quantized_any per projection with is_local_expert gating (line 267-275); the EXL3 rewrite happens EARLIER in weight_map/exl3_materialize.rs:266-414 (wants_nvfp4_triplet routes .mlp.experts./.mlp.shared_expert. -> BF16 transient -> NVFP4 triplet). Native serving gate: exl3_native_serves (exl3_materialize.rs:83, currently lm_head only) + exl3_native_supported (K in {2,4} only — a concurrency invariant, line 102-107); expanding to experts means adding the prefix predicate, skipping the rewrite, and teaching load_moe_qwen35 (or a sibling) to produce Vec<Exl3Weight> + an ExpertWeight-null placeholder MoeWeights. ExpertPtrSet enum (tables.rs:82) is the intended dispatch enum to extend with an Exl3 arm."
        },
        {
          "name": "exl3_mgemm launch contract (the call that replaces gate/up/down)",
          "file_line": "crates/spark-model/src/layers/ops/exl3_matmul/mgemm.rs:26-136, exl3_matmul.rs:33-118, kernels/gb10/common/exl3_matmul.cu:30-60",
          "detail": "exl3_mgemm(gpu, a fp16 [bszm_in, m, k] (bszm_in==1 broadcasts one activation to all slots), b_list/suh_list/svh_list [bszm] u64 device-ptr arrays, c [bszm_out, m, n] f16 or f32 (use c_fp32=true for MoE — fp32 grouped reduction), m,k,n, k_bits, cb, locks (EXL3_LOCKS_BYTES=4,202,760 B, alloc+zero ONCE via exl3_locks_alloc, kernels self-reset), a_had fp16 scratch with capacity >= bszm*m*k halves (ASSERTED; undersize = silent OOB), b_indices Option (u32 expert ids per slot — REQUIRED when min_index>=0), b_weights Option (f32 per slot; triggers grouped weighted reduction: each contiguous group of bszm/num_tokens slots is summed into row-block t of C — num_tokens must divide bszm), bszm_in, bszm_out, min_index/max_index (EP expert-range filter; min_index>=0 caps bszm<=128 and uses module-scope __device__ globals v_indices/v_weights/bszm_sync — SERIALIZE filtered launches per device), num_tokens, size_n_list/c_list (standalone per-matrix-width mode, cannot combine with grouping/filter/weights), force_shape, sm_count (resolve once at construction), stream. COOPERATIVE launch + 90KB dynamic smem (raise_smem_once). Kernel symbols exl3_mgemm_k{K}_cb{CB}_sh{2..4}_{f16|f32}: ONE k_bits per launch. GEMV cap: EXL3_GEMV_MAX_M=8 (single-matrix only; no multi-matrix gemv). Working reference implementation of the whole native pattern (construction-time locks/A/C slabs, bf16<->f16 converters, row-keyed scratch, graph veto): model/lm_head_exl3.rs:100-263. Contract doc also at .research/native_port_map.json (read:gemm-core)."
        },
        {
          "name": "Shared expert + router gate call sites",
          "file_line": "moe/forward.rs:145-177+685-737, forward_prefill.rs:161-173+420-495, forward_prefill_phase.rs:22, forward_batched_gate.rs, forward_prefill_router.rs",
          "detail": "Router gate: decode C=1 forward.rs:149-177 (w4a16_decode_gemv or dense_gemv, router_logits_n=num_experts=512); batched decode via batched_gate_logits (forward_batched.rs:63, impl in forward_batched_gate.rs); token-major forward_token_major.rs:66-92; prefill forward_prefill.rs:180-221 via router_gate_gemm_dense (forward_prefill_router.rs — selection numerics pinned to the scalar kernel, 2026-08-12 BFCL regression). Shared expert: decode/K2/K3/token-major = FUSED into the moe_expert_*_shared kernels as the extra grid slot (args &self.weights.shared_expert.gate_proj/up_proj/down_proj as QuantizedWeight; NULL-guarded in moe_shared_expert_fused.cu:83); prefill = run_shared_expert_prefill (forward_prefill_phase.rs:22, called forward_prefill.rs:162, W4A16 GEMMs into attn_output) then moe_batched_blend sigmoid-gate blend (forward_prefill.rs:439-449, or post-all-reduce at :483-494). A native EXL3 shared expert therefore requires UNFUSING it from the decode kernels — precedent: has_mixed_bf16_shared_expert + run_bf16_shared_expert (forward.rs:629-641) runs shared as a separate pass after the routed kernels. Routing probs are applied ONLY at: moe_weighted_sum_blend (decode), moe_weighted_sum_blend_batch2/3, moe_weighted_sum_blend_prefill (token-major), moe_unpermute_reduce_indexed (grouped prefill) — or foldable into mgemm's b_weights epilogue."
        },
        {
          "name": "Scratch/buffers + graph-capture constraints",
          "file_line": "crates/spark-runtime/src/buffers/accessors.rs:58-87, buffers/sizes.rs:163-250, model/trait_impl/decode_a.rs:251-263, decode_a2.rs:282+401, lm_head_exl3.rs:56-61+153-172",
          "detail": "Available arena buffers: scratch (>=64KB; MoE routing region 2*m*top_k*4 then metadata — do NOT grow casually, metadata layouts are offset-coupled), expert_gate_out/expert_up_out = k_max*top_k*inter*2 B each (k_max = max(max_batch_tokens,3)), expert_down_out = k_max*top_k*H*2 B, gate_logits (also hosts the sort outputs), moe_output, plus decode-scratch aliases logits/ssm_qkvz/attn_output for the shared expert (logits aliasing warning forward.rs:349-352). NO fp16 or fp32-C slabs exist for MoE — the exl3 pattern (lm_head_exl3.rs:153-172) allocates its own at CONSTRUCTION: locks via exl3_locks_alloc (once per device, shareable), A fp16 slab, C slab; nothing on the hot path may alloc or sync (901 playbook). A_had needs bszm*m*k halves: decode C=1 = 10*2560 halves = 50KB; a 128-slot prefill batch at m rows/slot needs bszm*m*2560 halves — size to the chosen slot-batch envelope at construction. Graph capture: single-seq decode captures the WHOLE forward including MoE (decode_a.rs:225-263); multi-seq batched decode captures per padded_n (decode_a2.rs:401 graph_capture: use_graphs); MTP verify contexts run with graph_capture:false (decode_b.rs:461,487). Cooperative launches are NOT capturable (CUDA_ERROR_STREAM_CAPTURE_UNSUPPORTED) — the exl3 lm_head vetoes decode graphs at BOTH decision points (decode_a.rs:261-263 `self.lm_head_exl3.is_none()`, decode_a2.rs:282 exl3_head_veto); a native MoE arm must add its own veto term, not rely on the lm_head coincidence. exact_tiles D2H in prefill is already gated off under ctx.graph_capture (forward_prefill_routed.rs:89)."
        },
        {
          "name": "EP: remote-expert representation + filtering",
          "file_line": "weight_map/expert.rs:85-97, atlas-core/src/config/methods.rs:129-147, moe_permute.cu:249-266, moe_shared_expert_fused.cu:108-110, moe/forward.rs:674-737",
          "detail": "Remote experts are ExpertWeight::null() at load (load_moe_qwen35 line 267-269, gated by config.is_local_expert(e); local_expert_range = contiguous [rank*E/W, (rank+1)*E/W), last rank takes remainder). Null pointers flow into the ptr tables; per-step filtering is IN-KERNEL: fused decode kernels check B_packed==0 -> write zero output (moe_shared_expert_fused.cu:109); grouped prefill worklist builder skips M_e<=0 OR null-weight experts (moe_permute.cu:262-266, the #781 fix tested rows only); grouped w4a16 kernels have per-tile B_exp==0 guards; prefill zeroes expert_* destinations first so unwritten remote rows read as 0 through unpermute. Outputs are summed by all-reduce (forward.rs:705-712 decode, forward_prefill.rs:455-495 prefill; graph_capture uses blocking comm.all_reduce). The native mgemm arm replaces the null-check convention with min_index/max_index = local_expert_range (b_indices required, bszm<=128) — semantically identical: filtered slots contribute nothing, all-reduce sums ranks. CUTLASS-EP precedent + validation state: memory qwen4exp-cutlass-moe-ep2-passes."
        }
      ],
      "design": "Recommended implementation, in dependency order:\n\n1. Loader + weight model (new expert format arm).\n   - weight_map/exl3_materialize.rs: extend exl3_native_serves() to cover `.mlp.experts.N.{gate,up,down}_proj` (and optionally `.mlp.shared_expert.*`) when ATLAS_EXL3_NATIVE=1 AND exl3_native_supported(w) holds for the tensor; keep the NVFP4-triplet fallback for out-of-envelope tensors. Decide the K envelope explicitly: mgemm/gemm cover K in {2,3,4,5,6,8}; the current {2,4} restriction is a GEMV-concurrency invariant that does not bind the MoE arm (it never uses gemv), so gate experts on `matches!(k_bits, 2..=6|8) && cb in {Mcg,Mul1} && dims %128==0` but require ONE k_bits per (layer, projection) group or bucket experts by K.\n   - weight_map/ssm_qwen35.rs load_moe_qwen35: add a native-EXL3 branch (mirroring load_moe_qwen35_fp8_experts's shape) that returns MoeWeights with null ExpertWeights PLUS Vec<Option<Exl3Weight>> per projection (None = remote/EP, mirroring is_local_expert at line 267). Shared expert: keep NVFP4 (materialized) in milestone 1 so all fused decode kernels keep their shared-expert slot; serve it natively later via a separate exl3_gemm pass (run_bf16_shared_expert precedent).\n   - layers/moe/tables.rs + ptr_table_build.rs: add Exl3ExpertPtrTable { trellis_list, suh_list, svh_list: DevicePtr /*[num_experts] u64*/, k_bits: u32, cb: u32 } and build_exl3_ptr_table(&[Option<Exl3Weight>]) (null u64 for remote — never dereferenced thanks to index filtering). Store Option<[Exl3ExpertPtrTable;3]> on MoeLayer (init.rs), plus a construction-time Exl3MoeState: shared locks (exl3_locks_alloc, one per device — can share the lm_head's), A fp16 ingress slab, A_had slab (may alias A per the upstream contract), C fp32 slab sized for the largest arm, sm_count.\n\n2. Decode C=1 (moe/forward.rs, new branch ahead of the NVFP4 arm, same shape as the bf16/fp8 arms):\n   - exl3_bf16_to_f16(expert_input, a_f16, H) once (H=2560, post-norm activations — fp16-safe).\n   - gate: exl3_mgemm(a=a_f16, bszm_in=1 (broadcast), b_indices=indices_dev, bszm_out=top_k=10, m=1, k=2560, n=640, c_fp32=true, min_index/max_index=local_expert_range under EP else -1) -> C_gate f32 [10,1,640]. up: identical second call. (A fused gate+up would need a kernel change; two mgemm calls is the milestone-1 form.)\n   - activation: convert or compute silu in f32 — simplest: exl3_f32_to_bf16 both Cs into expert_gate_out/expert_up_out ([top_k, 640] BF16 slot-major, exactly today's layout) then reuse ops::silu_mul (moe_act_mul) unchanged (total 10*640 elements).\n   - down WITH the routing weights folded: bf16->f16 the silu output, then exl3_mgemm(b_indices=indices_dev, b_weights=weights_dev, num_tokens=1, bszm_in=top_k, bszm_out=1, m=1, k=640, n=2560, c_fp32=true) -> C [1,1,H] = sum_k w_k * down_k — this REPLACES moe_weighted_sum_blend's routed half. Keep the existing shared-expert kernels + blend by writing the routed sum into expert_down_out-equivalent and calling moe_weighted_sum_blend with top_k=1/weight=1, or (cleaner) add a small blend variant that takes a pre-reduced routed vector. EP unchanged: zero-shared blend -> all_reduce -> moe_batched_blend.\n\n3. Decode n=2..8 (trait_decode_multi_seq ladder + ffn.rs mirror): one arm serves all n — same three mgemm calls with bszm = n*top_k (<=80, under the 128 filter cap), num_tokens=n, b_weights=weights_dev ([n,top_k] f32 token-major = mgemm's contiguous-group contract), A = fp16 [n*top_k? no — bszm_in=n*top_k requires replicating each token's row per slot; cheaper: gather during ingress with a tiny replicate kernel, or pass bszm_in=n*top_k with a strided copy]. Practical route: replicate token rows into A [n*top_k, 1, 2560] fp16 during the bf16->f16 ingress (one trivial new plain kernel), then gate/up mgemm (bszm=n*top_k, m=1), silu_mul, down mgemm with num_tokens=n -> C [n,1,H]. Wire it where forward_token_major_decode dispatches (presence-gated on the exl3 tables, like the bf16/fp8 gates), falling back to nothing else — there is no NVFP4 copy to fall back to when experts are served natively, so this arm must cover forward, forward_k2/k3, forward_batched, and forward_token_major entry points (forward_k2/k3 can simply delegate to the generic n-token arm, as their LoRA/BF16 fallbacks already delegate to forward_batched).\n\n4. Prefill (forward_prefill_routed.rs, new leading branch like the E8M0 one): keep steps 1-3 (router GEMM, topk_batched, moe_sort_by_expert) untouched. Two viable shapes — implement (a) first:\n   (a) Per-expert GEMM over sorted rows: D2H expert_offsets (exact_tiles/copy_d2h_on_stream precedent, or reuse the host Vec<i32> the CUTLASS path returns), gather rows expert-contiguously with the retained moe_permute_tokens kernel (+ bf16->f16 ingress on the permuted [te, 2560] slab), then for each local expert with rows>0 launch exl3_gemm (m=rows, cooperative; rows>8 fine) for gate and up into the SORTED expert_gate_out/expert_up_out layout, ops::silu_mul unchanged, per-expert exl3_gemm down into sorted expert_down_out, then apply_expert_lora_prefill_down + moe_unpermute_reduce_indexed + shared blend + EP all-reduce ALL UNCHANGED. Serial per-expert launches are acceptable at qwen4_exp shapes (typically 100-300 active experts/layer at real chunk sizes); the D2H sync mirrors the existing exact_tiles default-on behavior for NVFP4.\n   (b) mgemm slot-batching (no D2H): loop ceil(n/12) batches of 12 tokens (12*10=120<=128 slot filter cap) with b_indices/b_weights slices and num_tokens=12, writing [12,1,H] blocks — fewer launches but replicated-A ingress and per-batch grid underutilization; benchmark against (a) before committing.\n\n5. Graph veto + serialization: add `|| any layer serves EXL3 experts` to the decode-graph decisions at decode_a.rs:261-263 and decode_a2.rs:282 (mirror the lm_head_exl3 veto and its log line). All MoE layers run serially on one stream, so the filtered-mgemm module-global hazard and the shared locks buffer are safe today; document that a second-stream co-dispatch of any exl3 gemm/mgemm requires per-stream locks.\n\n6. File-touch list: weight_map/exl3_materialize.rs (serve-set + envelope), weight_map/ssm_qwen35.rs (native expert loader), weight_map/moe.rs or a sibling (carry Vec<Option<Exl3Weight>> to the layer), layers/moe/tables.rs, layers/moe/ptr_table_build.rs, layers/moe/init.rs (table build + Exl3MoeState slabs + kernel-free construction), layers/moe/mod.rs (fields), layers/moe/forward.rs, forward_k2.rs, forward_k3.rs, forward_batched.rs, forward_token_major.rs (dispatch gates), layers/moe/forward_prefill_routed.rs (native branch), layers/ops/exl3_matmul.rs (possibly a replicate-ingress helper), model/trait_impl/decode_a.rs + decode_a2.rs (graph veto), factory/build.rs (state construction/wiring, mirroring lm_head_exl3 at build.rs:820-866).",
      "risks": [
        "Cooperative-launch vs CUDA graphs: native MoE experts run in EVERY captured decode graph path (single-seq decode_a.rs captures the whole ~130-kernel forward; multi-seq decode_a2 captures per padded_n). The veto today keys ONLY on lm_head_exl3.is_some(); enabling native experts without extending both veto sites will fail capture with CUDA_ERROR_STREAM_CAPTURE_UNSUPPORTED. Memory says decode graphs are speed-neutral on GB10 for this family, so the veto is cheap — but it must be explicit.",
        "mgemm index-filtering cap: min_index>=0 caps bszm at 128 slots. Decode (<=80 slots at n=8) fits; prefill under EP does NOT (te = n*10 slots), so the prefill arm must either loop <=128-slot batches or use the per-expert exl3_gemm form where EP filtering is a host-side skip of remote experts (offsets are known host-side after the D2H). Also the filtered path's module-scope __device__ globals forbid overlapping filtered mgemm launches on one device — safe on today's single serial compute stream, broken by any future co-dispatch (Q12 co-dispatch runs prefill+decode concurrently: verify MoE never overlaps a second exl3 launch, or give each stream its own locks AND accept the globals hazard is not stream-local — filtered mgemm under co-dispatch is simply unsafe).",
        "One k_bits per launch: exl3_mgemm dispatches exl3_mgemm_k{K}_... — all experts in a call must share k_bits (and cb). A 2.05bpw EXL3 export may mix per-expert K (EXL3 optimizes bit allocation); the loader must verify uniformity per (layer, projection) and either bucket experts by K (multiple mgemm calls with disjoint b_indices subsets + zero-fill semantics via b_weights grouping — awkward) or refuse loudly. Check the actual checkpoint before designing for uniform K.",
        "fp16 A ingress saturation: |v|>65504 -> inf. Post-norm MoE inputs are safe (lm_head precedent), but the DOWN projection's A is silu(gate)*up intermediates — verify their range on real activations (inter=640 accumulations at 2.05bpw could spike); c_fp32=true protects C but not A. Fallback: clamp in the ingress converter.",
        "Scratch overlap hazards: the sort outputs live INSIDE gate_logits and the shared expert decode scratch aliases logits()/ssm_qkvz()/attn_output() (documented corruption history, forward.rs:349-352). New A/C slabs must be OWNED allocations (construction-time), not arena aliases — expert_gate_out etc. are BF16-sized and cannot hold fp32 C for the gate/up (which need te*640*4 bytes = 2x the bf16 slab). At max_batch_tokens=4096, C_f32 for gate+up = 2*4096*10*640*4 = ~210 MB and A fp16 permuted = 4096*10*2560*2 = ~210 MB — size the prefill arm's slabs deliberately (chunking the prefill loop bounds this) and account them against the util pledge (dflash-oom-hazard precedent: allocations outside the pledge wedge the box).",
        "Per-expert prefill launch count: shape (a) launches ~2-3 cooperative GEMMs per ACTIVE expert per layer; at 512 experts with broad routing (long chunks activate most experts) that is potentially ~1000+ cooperative launches/layer vs 2 grouped launches today — the small-M grouped-GEMM economics that killed per-token grouped decode cut the other way here. Benchmark early at real chunk sizes (1024/4096) before committing; the mgemm slot-batch form or a future in-kernel expert-loop (upstream's MoE scheduler region in the locks buffer suggests the vendored kernel may already amortize) are the mitigations.",
        "MTP verify paths (forward_k2/k3) and the shadow-MoE draft head run these same MoeLayer entry points with graph_capture:false but on the primary stream between captured regions — the qwen4exp MTP work isolates side-forwards with a private BufferArena (memory: qwen4exp-mtp-scope); a native arm sharing one A/C slab across main+MTP forwards must key slabs per arena or per call-site row range (lm_head's scratch_row pattern) to avoid aliasing.",
        "EP semantics drift: today remote experts contribute EXACT zeros (null-guard writes or pre-zeroed buffers). The mgemm filter must be verified to produce zeros (not garbage) for filtered slots in the grouped reduction before trusting all-reduce sums; add a 2-rank parity test mirroring the CUTLASS EP=2 harness (memory: qwen4exp-cutlass-moe-ep2-passes, test precedent 93543fc19).",
        "Routing-prob application must happen exactly once: folding b_weights into the down mgemm AND leaving moe_weighted_sum_blend's routed sum in place would double-apply; the blend still must run for the shared-expert sigmoid gate. Bit-parity gate the C=1 path against the NVFP4 arm's structure (same indices/weights) and against the EXL3 reference dequant before wiring n>1."
      ]
    },
    "read:gdn-attn-routing": {
      "summary": "Serving qwen4_exp's GDN + attention dense linears natively from EXL3 trellis is feasible with NO kernel changes: every consumer either already runs per-projection GEMM/GEMVs into separate/offset output buffers (attention q/k/v/o) or is parameterized by an input row-stride that lets the fused-QKVZ concat be replaced by two packed weights writing a split arena (GDN). Upstream exllamav3 serves in_proj_qkv and in_proj_z as SEPARATE exl3 linears (py_arch_qwen4_exp.py:126-131) and q_proj as ONE interleaved-gate linear [2560->12288] (py_attn.py:229-244) — exactly the shapes in the checkpoint header (in_proj_qkv trellis [160,640,16K], q_proj svh [12288]). Atlas's gpu_concat_rows fuses only a memory layout, not math; the exl3_mgemm size_n_list/c_list mode (mgemm.rs:82-92) can even keep qkv+z (or q/k/v) in one cooperative launch with per-slot n and C pointers. TP is confirmed a no-op (Qwen4ExpWeightLoader::supports_tp() == false, qwen4_exp.rs:137-141; all shard_* helpers return the source pointer at tp=1). Memory win at K=2: GDN family 3.63 GB (BF16 4.15 GB -> 0.52 GB packed, 36 layers), attention family ~1.0 GB (12 layers, also removes the BF16->NVFP4 double-quant that attention decode suffers today), total ~4.6 GB on top of the expert win. Main hazards: the sampled export packs ALL GDN/attention dense linears at K=6 while exl3_native_supported() only admits K in {2,4} (the shared-locks GEMV/GEMM concurrency invariant), cooperative launches veto decode-graph capture (veto already keyed on the native lm_head), and every native site needs bf16->f16 ingress + f16(or f32)->bf16 egress conversion launches.",
      "contracts": [
        {
          "name": "GDN in_proj_qkv/in_proj_z load transform (today)",
          "file_line": "crates/spark-model/src/weight_loader/qwen35/load_layers/linear_attn_arms.rs:236-260 (bf16 default arm), :362-377 (NVFP4 A/B arm); crates/spark-model/src/weight_map/ssm_qwen35.rs:41-84 (load_ssm_qwen35); crates/spark-model/src/weight_map/fp8_lut.rs:387-408 (gpu_concat_rows)",
          "detail": "qwen4_exp default (ATLAS_QWEN4EXP_BF16_GDN unset => bf16_gdn=true, qwen4_exp.rs:232, arm dispatch :289) -> build_linear_attention_dense_bf16: load_ssm_qwen35 reads SEPARATE in_proj_qkv [10240,2560] and in_proj_z [6144,2560] BF16 (rewritten from trellis by materialize_exl3, exl3_materialize.rs:387-397), then gpu_concat_rows builds ONE fused [16384,2560] BF16 buffer (alloc + two copy_d2d) and frees the parts. NO quantization on this arm — the concat is purely a layout fusion so one GEMV/GEMM covers Q|K|V|Z. shard_gdn_qkvz_rows is a pointer-identity no-op at tp=1 (:227 comment, :256). out_proj stays BF16, installed as layer.out_proj_dense (:318-320). ssm.out_proj = QuantizedWeight::null(). The alternate NVFP4 arm (=0) additionally quantize_to_nvfp4 the concat + transpose_for_gemm (:426-441)."
        },
        {
          "name": "GDN qkvz forward consumers (today)",
          "file_line": "decode M=1: crates/spark-model/src/layers/qwen3_ssm/ssm_forward.rs:114-123 (dense_gemv, sequential_qkvz=true via new_sequential, init_sequential.rs:40) writing directly into ssm_deinterleaved; z_ptr = deinterleaved + (key_dim*2+value_dim)*2 = +10240*2 B (:184). batched decode M=2/3/4: trait_decode_batched.rs:382-392 (dense_gemv_batch2), :350-361, :324-335 (per-token dense_gemv into proj_dst rows of stride qkvz_size); M>8 fallback dense_gemm :525-535. prefill: trait_prefill_proj.rs:374-404 cublas_bf16_proj_dense (85-TFLOP cuBLASLt; the scalar dense_gemm fallback measured 63% of an 8.4s TTFT). conv1d consumes the buffer with an explicit in-stride arg = qkvz_size (trait_prefill_phase1.rs:140-155); Z is extracted by ONE pitched copy with src pitch qkvz_size (:191-201).",
          "detail": "Layout contract: [M, 16384] rows = [Q 2048 | K 2048 | V 4096->6144... actually Q|K|V = 10240 | Z = 6144] per token. All consumers take strides/offsets, none require the fused weight — only the fused OUTPUT row layout, and that only via parameterized strides."
        },
        {
          "name": "GDN out_proj forward consumers (today)",
          "file_line": "decode M=1: ssm_forward.rs:432-442 (dense_gemv on out_proj_dense, out [1,2560] contiguous); batched decode: trait_decode_batched.rs:914-925 (dense_gemm, C [K,2560] contiguous); prefill: trait_prefill_helper.rs:120 (out_proj_dense arm)",
          "detail": "Simplest native target: single un-concatenated weight [2560 out, 6144 in], every call site writes a contiguous [m, 2560] C. n=2560, k=6144 both %128==0. A native arm slots in exactly where out_proj_fp8w already does (ssm_forward.rs:414-431 precedent)."
        },
        {
          "name": "Attention q/k/v/o load transform (today)",
          "file_line": "crates/spark-model/src/weight_loader/qwen35/load_layers/attention_arms.rs:104-181 (Standard/Fp8Dequanted/Bf16Raw arm — this checkpoint detects Standard: experts got .weight_scale from materialize_exl3, nvfp4_detect.rs:155-185), transposed prefill twins :207-221",
          "detail": "Per projection: dense_auto BF16 [n,k] -> shard_dense_bf16 (no-op tp=1) -> quantize_to_nvfp4 (runtime double-quant). q/k/v keep the BF16 dense copy resident in AttentionWeights (q_proj=q_dense etc, :167-178) PLUS NVFP4 PLUS transposed-NVFP4 twins; o_proj is NVFP4-only. NO concatenation anywhere — q, k, v, o are four independent weights. q_proj is [12288, 2560]: nq=24 x hd=256 x 2 (gated, interleaved [Q|gate] per head); k/v are [512, 2560] (2 KV heads); o [2560, 6144]."
        },
        {
          "name": "Attention forward consumers (today)",
          "file_line": "decode: qwen3_attention/decode/attention_forward.rs:165-176 (w4a16_gemv_qg fused GEMV+QG-deinterleave; dense fallback :179-200 = dense_gemv + separate deinterleave_qg — the exact shape a native GEMV needs), attention_forward_kv.rs:79-90 (w4a16_gemv_dual K+V, separate k_out/v_out pointers = offsets into one qkv buffer, attention_forward.rs:274-275), attention_forward_oproj.rs:80-89 (nvfp4_decode_gemv, C contiguous). prefill: prefill/paged_qkv.rs:71-141 — Q into buffers.qkv_output(), K into buffers.ssm_qkvz(), V at +num_tokens*kv_dim offset: three separate GEMMs already; nvfp4_t arms :324-349, BF16 fallback :362-396; o: paged_oproj.rs:83,242. cache_skip_qkv.rs mirrors it.",
          "detail": "Attention is ALREADY per-projection with independent C destinations — native exl3 gemv/gemm is a drop-in dispatch arm per site, no output-layout surgery. Gated Q: kernel writes interleaved [Q|gate] exactly as the checkpoint packs it; reuse the existing deinterleave_qg step (upstream does the same: py_attn.py:552-566 ext.deinterleave_qg after one q_proj forward)."
        },
        {
          "name": "Upstream structure (exllamav3 reference)",
          "file_line": ".research/exllamav3_ref/py_arch_qwen4_exp.py:126-131 (key_qkv/key_z/key_o separate GDN linears); py_attn.py:229-244 (q_proj out=nq*hd*2 interleaved_gate), :252-283 (separate k/v), :295-306 (o), :425-465 (multi_kv/multi_qg MultiLinear fusion via exl3_mgemm ONLY when out_features equal), :552-599 (project_qkv: q_proj then deinterleave_qg; mgemm path for bsz*q_len<=32)",
          "detail": "Upstream serves in_proj_qkv and in_proj_z as SEPARATE exl3 linears (checkpoint packs separate trellis/suh/svh per prefix — fusing packed trellis weights is impossible: each has its own suh/svh rotation and svh length differs). It fuses only equal-n pairs (k+v) through the pointer-table mgemm. So two separate native gemms replacing Atlas's concat is exactly the upstream-sanctioned serving shape; the checkpoint header confirms: in_proj_qkv {suh[2560], svh[10240], trellis[160,640,16K]}, in_proj_z {svh[6144], trellis[160,384,16K]}, q_proj svh[12288], k/v svh[512], o_proj svh[2560] (.research/ckpt_meta/shard1_header.json)."
        },
        {
          "name": "Native kernel/wrapper contracts",
          "file_line": "crates/spark-model/src/layers/ops/exl3_matmul.rs:189-233 (exl3_gemm: C fp16/fp32 [m,n] CONTIGUOUS, no ldc), :280-347 (exl3_gemv: m<=8, K in 2..=4 only, n%128, cooperative), :55 (EXL3_LOCKS_BYTES=4,202,760 shared per device), :58 (EXL3_GEMV_MAX_M=8); mgemm.rs:26-136 (exl3_mgemm: size_n_list+c_list per-slot widths/pointers mode, mutually exclusive with filtering/weights/token-grouping :82-92; A_had capacity assert bszm*m*k :58-62); converters mgemm.rs:158-188 (bf16->f16 ingress, f16/f32->bf16 egress)",
          "detail": "No C row-stride exists, so a fused [M,16384] qkvz output CANNOT be written by two launches at M>1 — resolved by splitting the arena (qkv block [M,10240] then z block [M,6144]) since every consumer takes a stride/offset, or by ONE mgemm launch with bszm=2, size_n_list=[10240,6144], c_list=[qkv_buf,z_buf] (requires equal k_bits across slots — true per family in the export). A_had may alias A for gemm/gemv (lm_head precedent, lm_head_exl3.rs:239); mgemm needs a real bszm*m*k slab."
        },
        {
          "name": "K-bits envelope vs checkpoint reality",
          "file_line": "crates/spark-model/src/weight_map/exl3_materialize.rs:93-107 (exl3_native_supported: K in {2,4} ONLY — concurrency invariant, not kernel gap; kernels compiled for K 2,3,4,5,6,8 per exl3_matmul.rs:90-100); .research/ckpt_meta/shard1_header.json (sampled export: experts K=4, ALL linear_attn.* and self_attn.* dense linears K=6, shared_expert K=6, indexer K=4)",
          "detail": "The sampled export's GDN/attention linears are K=6 — outside both the gemv envelope (2..=4) and exl3_native_supported ({2,4}); at rows<=8 they route to the split-K cooperative GEMM whose shared locks buffer is unsafe under co-dispatched concurrent small-row launches. Serving THIS export's dense linears natively requires either per-stream/per-site locks buffers or compiling GEMV instances for K=6. A 2.05bpw export (K=2/3 dense) fits the gemv envelope but K=3 must be added to exl3_native_supported with the same locks decision (gemv_cfg_blackwell returns -1 for K=3 at many shapes, exl3_matmul.rs:264-265 — falls to GEMM)."
        },
        {
          "name": "TP confirmation",
          "file_line": "crates/spark-model/src/weight_loader/qwen4_exp.rs:137-141 (supports_tp() = false); linear_attn_arms.rs:225-227,256-259 (shard_gdn_* return source pointer at tp=1); attention_arms.rs:75-76,113-114,148-150 (tp_size==1 early-return / pointer-identity)",
          "detail": "qwen4_exp cannot load under TP at all; EP=2 shards only the MoE experts (dense GDN/attention weights replicated per rank). All shard_dense/shard_gdn paths are byte-identity no-ops — a native route needs zero sharding logic, but must keep working when EP filtering runs the expert mgemm (min_index>=0 uses module-scope __device__ globals: serialize filtered mgemm launches per device, exl3_matmul.rs:30-31)."
        },
        {
          "name": "MTP-head projections",
          "file_line": "crates/spark-model/src/weight_loader/qwen4_exp.rs:452-464 (load_mtp_weights -> Ok(None) on this branch); .research/exllamav3_ref/py_qwen4_exp_mtp.py:53-72 (fc_hidden/fc_embedding [2560->2560] exl3-quantized, qbits_key=mtp_bits)",
          "detail": "MTP is NOT wired on wip/exl3-research (v1 dropped it; the working MTP lives on wip/qwen4exp-working). When ported: upstream quantizes fc_hidden/fc_embedding as exl3 linears (~13 MB BF16 each — negligible memory) plus a full extra decoder block whose GDN/attn/MoE would reuse whatever native routes this milestone builds. Out of scope for the memory win."
        },
        {
          "name": "Scratch/locks/graph-capture story",
          "file_line": "crates/spark-model/src/model/lm_head_exl3.rs:34-60 (ownership model: locks shared per device, GEMV never touches locks, GEMM does; A/C fp16 slabs allocated once, keyed by destination row for disjoint concurrent streams), :154 (exl3_locks_alloc once); decode-graph veto: model/trait_impl/decode_a.rs:263 and decode_a2.rs:282 (capture disabled when lm_head_exl3 installed — cooperative launches are not capturable)",
          "detail": "For layer-site expansion: ONE locks buffer + ONE f16 A-scratch slab per model (not per layer) sized max_prefill_chunk*max_k halves suffices for the serialized primary stream; co-dispatched prefill streams each run rows>8 GEMMs in EVERY GDN/attn layer, so the single shared locks buffer becomes a real race — allocate locks (4.2 MB) and A/C staging per co-dispatch stream, or serialize native prefill GEMMs. Egress: kernels emit f16/f32 C, consumers read BF16 -> every site needs the exl3_f16_to_bf16 (or write-to-staging + convert) hop; GDN decode adds ~3 extra elementwise launches/layer on an already launch-bound path. Decode graphs: the existing veto is keyed on the native lm_head, which is always installed when ATLAS_EXL3_NATIVE=1, so GDN/attn native decode is automatically capture-free; decode graphs are speed-neutral on GB10 (lm_head_exl3.rs:58-60)."
        }
      ],
      "design": "Ordered next milestone (memory saved at K=2 computed as params*K/8 packed vs params*2 BF16; suh/svh add <0.1%):\n\nSTEP 1 — GDN out_proj, 36 layers [2560 out x 6144 in], 15.73M params/layer: BF16 31.46 MB -> K=2 3.93 MB, saves 27.5 MB/layer = 0.99 GB (K=3: 0.87 GB; at the sampled export's K=6: 0.66 GB). Integration: (a) add \"linear_attn.out_proj\"-suffixed prefixes to exl3_native_serves (weight_map/exl3_materialize.rs:83-85) gated on exl3_native_supported; (b) resolve Exl3Weight::from_store in build_linear_attention_dense_bf16 (linear_attn_arms.rs:210) instead of load_proj for out_proj, store as new field out_proj_exl3: Option<Exl3Weight> on Qwen3SsmLayer (layers/qwen3_ssm/mod.rs:67 area); (c) new dispatch arms ABOVE out_proj_dense at ssm_forward.rs:432, trait_decode_batched.rs:914, trait_prefill_helper.rs:120 (+ the trait_decode_multi_seq twin): bf16->f16 into shared A slab, exl3_gemv (m<=8, C f32 single-row slab like project_single_fp32) else exl3_gemm, then exl3_f16_to_bf16 into the existing destination. C is [m,2560] contiguous everywhere — zero layout work. This is the smallest end-to-end proof that layer-site native dispatch works under decode/batched/prefill/co-dispatch.\n\nSTEP 2 — GDN in_proj_qkv + in_proj_z, 36 layers, 26.21M + 15.73M params/layer: BF16 52.43+31.46 MB -> K=2 6.55+3.93 MB, saves 73.4 MB/layer = 2.64 GB (whole GDN family incl. step 1: 3.63 GB at K=2, 3.37 at K=3, 2.59 at K=6). Integration: keep the two weights as SEPARATE Exl3Weights (never concat packed trellis — each has its own suh/svh and different n); skip gpu_concat_rows on the native arm. Forward: decode M=1 needs NO buffer change — two gemvs write C at deinterleaved and deinterleaved+10240*2 (ssm_forward.rs:114-123, offsets already computed at :178-184). For M>1 (batched decode arms trait_decode_batched.rs:297-536, prefill trait_prefill_proj.rs) re-lay the arena as qkv block [M,10240] followed by z block [M,6144]: conv1d already takes the input row-stride as an argument (pass 10240 instead of qkvz_size, trait_prefill_phase1.rs:152 and the batched-decode conv sites), and the Z pitched copy (:191-201) becomes a contiguous copy; touch each consumer offset (ssm_forward, phase1, decode_batched, decode_multi_seq). Either two exl3_gemm launches, or ONE exl3_mgemm with bszm=2 + size_n_list/c_list (mgemm.rs:82-92; requires equal K per pair — assert at load) to halve launch count. No perf cliff expected at decode (two packed reads of 10.5 MB vs one 83.9 MB BF16 read is a ~8x bandwidth cut); prefill replaces an 85-TFLOP cuBLASLt BF16 GEMM with the cooperative trellis GEMM — measure TTFT, keep ATLAS_QWEN4EXP_BF16_GDN=1-style fallback env to A/B.\n\nSTEP 3 — Attention q/k/v/o, 12 layers: q [12288x2560] 31.46M, k/v [512x2560] 1.31M each, o [2560x6144] 15.73M = 49.8M params/layer; today resident approx BF16 q/k/v (68.2 MB) + NVFP4 + transposed twins (~28 MB) ~= 96 MB/layer; native K=2 = 12.45 MB/layer -> saves ~1.0 GB total AND removes the runtime BF16->NVFP4 double-quant on the 12 attention layers (fidelity win — decode currently reads the requantized NVFP4). Integration is per-site drop-in because outputs are already separate: decode Q = gemv writing interleaved [Q|gate] then existing deinterleave_qg (mirror the dense fallback at attention_forward.rs:179-200); K/V = two gemvs into k_out/v_out (attention_forward_kv.rs:105-138 shape) or one mgemm multi_kv (equal n=512, upstream precedent py_attn.py:443); O = gemv/gemm into contiguous o_out (attention_forward_oproj.rs, paged_oproj.rs). Prefill: new arms in prefill_one_proj (paged_qkv.rs:157+) and cache_skip_qkv.rs. Drop the resident BF16 q/k/v denses and both NVFP4 copies on this arm.\n\nSTEP 4 (defer) — MTP fc_hidden/fc_embedding: not loaded on this branch; negligible memory; revisit when MTP is ported.\n\nCross-cutting prerequisites for all steps: (1) widen exl3_native_supported per-family — decide the locks-concurrency question first: allocate a per-co-dispatch-stream locks buffer + A/C staging (4.2 MB + slabs each) in ForwardContext/BufferArena rather than relying on the 'wide callers never overlap' assumption that layer sites break; (2) plumb {locks, sm_count, a_f16 slab, c staging} into Qwen3SsmLayer/Qwen3AttentionLayer construction (mirror lm_head_exl3.rs:106-196); (3) loader probes: dense_auto/quantized_any already bail with a routed-dispatch error if a kept-native prefix leaks to a BF16 consumer (weight_map/quant_helpers.rs:292-305) — extend exl3_native_serves only in lockstep with each step's dispatch arm; (4) per-tensor K varies by export — read K from the weight, never assume 2.",
      "risks": [
        "K-bits mismatch: the sampled export (.research/ckpt_meta/shard1_header.json) packs ALL GDN/attention dense linears at K=6, outside both the GEMV kernel envelope (exl3_gemv accepts K 2..=4 only, exl3_matmul.rs:301) and exl3_native_supported's {2,4}; K=6 decode-size calls fall to the cooperative split-K GEMM, which shares one locks buffer — unsafe under co-dispatched concurrent small-row launches. Widening the envelope (per-stream locks or K=6 GEMV instances) is a prerequisite for THIS export; a 2.05bpw export likely lands K=2/3 where only K=3 needs admitting.",
        "Shared locks buffer race: unlike the lm_head (one call site, exclusivity argued in lm_head_exl3.rs:47-54), GDN/attention native GEMMs run in EVERY layer of co-dispatched prefill streams — the single per-device locks buffer WILL be hit concurrently. Must allocate per-stream locks (4.2 MB each) or serialize; silent corruption otherwise.",
        "Prefill throughput cliff: the BF16 GDN QKVZ prefill currently runs cuBLASLt at ~85 TFLOPS (trait_prefill_proj.rs:374-404 measured history — the scalar fallback once cost 63% of TTFT); the cooperative trellis GEMM is Marlin-style and unproven at M=2048 x N=10240 on GB10. Keep an env A/B (materialized-BF16 arm) until TTFT is measured; memory win then requires choosing packed-only residency.",
        "f16 C egress + activation ingress: every native projection adds bf16->f16 and f16->bf16 elementwise launches; GDN decode is already launch-bound (~1000 kernels/token family finding). Batch the conversions (one ingress per layer covers qkv+z+ba inputs since they share `normed`) or the per-token decode win shrinks.",
        "Cooperative launches are not graph-capturable: fine today (veto keyed on lm_head_exl3, decode_a.rs:263 / decode_a2.rs:282, and ATLAS_EXL3_NATIVE=1 always installs the native head), but the veto condition should be made explicit for layer-site weights so a future lm_head opt-out cannot silently re-enable capture over GDN cooperative kernels (CUDA 900 class).",
        "mgemm expert-filtered launches use module-scope __device__ globals (exl3_matmul.rs:30-31): GDN/attention native launches on side streams must never interleave with a min_index-filtered MoE mgemm on the same device without serialization.",
        "Arena re-layout (qkv block + z block) touches every fused-QKVZ consumer offset across four decode/prefill files (ssm_forward.rs, trait_prefill_phase1.rs, trait_decode_batched.rs, trait_decode_multi_seq/*); an offset missed under one dispatch arm reproduces the historical NULL-slot/stride bug class (CUDA 700 on first K=4 verify) — gate each arm behind the same env and A/B against production config per the decode-nondeterminism working rule.",
        "ATLAS_GDN_BF16_WEIGHTS / ATLAS_FP8_ROWWISE / CUTLASS lever shadowing: trait_prefill_proj.rs is a priority chain with documented shadow warnings; the native arm's position must be chosen deliberately (above dense, below explicit precision levers) and logged, or existing A/B levers silently stop measuring what they claim."
      ]
    },
    "read:scope-accounting": {
      "summary": "Cross-cutting review for serving qwen4_exp's 512 routed experts (+shared expert) and later the GDN/attention dense linears directly from packed EXL3 trellis on branch wip/exl3-research. Key findings: (1) the existing gate machinery (exl3_native_serves/exl3_native_supported as the single source of truth shared by materializer and builder) extends cleanly to an ATLAS_EXL3_NATIVE_MOE=1 layer, and the skip_routed_experts=true precedent in load_moe_qwen35 is the right loader handoff (NULL NVFP4 experts + a separate Exl3 expert vec), NOT a new arm inside quantized_any — quantized_any already hard-errors on kept-packed prefixes (nvfp4_detect.rs:234-244) which becomes the transition tripwire; (2) packed trellis at K=2 is 416,004 B/projection vs 921,604 B NVFP4 → 30.67 GB vs 67.95 GB for 512 experts×48 layers×3 projections (15.34 vs 33.97 GB at EP=2), ptr tables are trivial (~1.8 MB), but mgemm slot scratch (A staging + A_had + fp32 C) is the real sizing concern and must be slot-batched (~130 MB at 512-token×top-10 batches) and placed inside the BufferArena/util pledge; (3) EP load-skip already drops remote experts' .trellis/.suh/.svh/.mul1 (pinned by test), and remote experts MUST be encoded as B_indices=-1 slots (the reduction epilogue skips negative indices; NULL ptr-table entries would sum stale scratch — wrong), which also sidesteps the 128-slot filter cap and the module-scope __device__ globals entirely; (4) with gates off, behavior is byte-identical because the materializer's keep-vs-rewrite branch is exclusive; (5) teardown: kept-packed tensors are freed by the adopted WeightStore's release (weights LAST), ptr tables/locks/scratch follow either the documented sweep_unreleased backstop or (better) a ModelResource; there is no tag_alloc_owner on this branch — the ledger is #[track_caller] call-site keyed, so attribution comes from allocating each class of buffer at its own named site; (6) all MoE mgemm launches are naturally serialized (single scheduler thread, stacked-GEMM prefill phase and decode both on the primary stream), one dedicated MoE locks buffer per model is safe, the lm_head already owns a separate locks buffer so cross-subsystem overlap cannot race, and the one-filtered-mgemm-per-device globals constraint is moot if filtering is never used (one process per device under EP; hot-swap is sequential).",
      "contracts": [
        {
          "name": "exl3_mgemm launch contract",
          "file_line": "crates/spark-model/src/layers/ops/exl3_matmul/mgemm.rs:26-135",
          "detail": "exl3_mgemm(gpu, a, b_list, c, m, k, n, k_bits, cb, c_fp32, locks, suh_list, a_had, a_had_capacity_elems, svh_list, b_indices, b_weights, bszm_in, bszm_out, min_index, max_index, num_tokens, size_n_list, c_list, force_shape, sm_count, stream). A fp16 [bszm_in,m,k] (bszm_in==1 broadcasts A[0], re-transformed per slot); A_had capacity MUST be >= bszm*m*k halves (asserted line 58-61; undersize = silent OOB); filtering (min_index>=0) caps bszm at 128 and requires b_indices (lines 63-71); b_weights requires num_tokens | bszm (lines 72-77); size_n_list/c_list is a standalone mode (lines 82-92). One K/cb per launch — all experts in a launch must share k_bits and codebook."
        },
        {
          "name": "mgemm negative-slot skip + weighted reduction",
          "file_line": "kernels/gb10/common/exl3_vendor/exl3_gemm_kernel.cuh:141-155, 261-290",
          "detail": "Slot j resolves mat_index = B_indices ? B_indices[j] : j; mat_index < 0 (or B_list[mat_index]==NULL) skips compute, BUT the final grouped reduction only skips a slot when B_indices[slot] < 0 (`if (!B_indices || B_indices[t*stride+j] >= 0) sum += ...`). Therefore remote/EP experts must be mapped to -1 IN B_INDICES, never as NULL pointer-table entries — a NULL entry with a non-negative index sums stale C scratch into the token's output."
        },
        {
          "name": "filtered-mgemm module-scope globals",
          "file_line": "kernels/gb10/common/exl3_vendor/exl3_gemm_kernel.cuh:83-85 (v_indices[128]/v_weights[128]/bszm_sync); wrapper doc crates/spark-model/src/layers/ops/exl3_matmul.rs:30-31",
          "detail": "min_index>=0 compaction/masking runs through module-scope __device__ globals — at most ONE in-flight filtered mgemm per loaded CUmodule (per AtlasCudaBackend/AtlasRegistry, i.e. per model instance per device). MAX_INDICES=128 slots. Both constraints disappear if Atlas never uses the filter and instead pre-maps remote experts to -1 host-side."
        },
        {
          "name": "locks buffer sharing rule",
          "file_line": "crates/spark-model/src/layers/ops/exl3_matmul.rs:17-19,55,65-71; crates/spark-model/src/model/lm_head_exl3.rs:47-54",
          "detail": "One per-device int32 buffer of EXL3_LOCKS_BYTES = 4,202,760 B, zeroed once, kernels self-reset; GEMM/mgemm use it (split-K spinlocks + barriers + MoE scheduler region), GEMV never touches it. At most one concurrent cooperative GEMM/mgemm per locks buffer. Exl3LmHead allocates its OWN locks (lm_head_exl3.rs:154), so a per-model MoE locks buffer is independent — cross-subsystem overlap cannot race."
        },
        {
          "name": "native-gate single source of truth",
          "file_line": "crates/spark-model/src/weight_map/exl3_materialize.rs:68-107 (exl3_native_enabled/exl3_native_serves/exl3_native_supported); re-derived in factory/build.rs:226-239",
          "detail": "materialize pass and model builder must agree via the same predicates. exl3_native_supported currently restricts K to {2,4} as a GEMV-concurrency invariant for small-row projections (lines 93-101) — experts need a SEPARATE MoE predicate (mgemm/gemm compile K 2,3,4,5,6,8 per ensure_k_cb, exl3_matmul.rs:90-99) plus a uniformity check (all local experts of a layer-projection share K and cb)."
        },
        {
          "name": "quantized_any native tripwire",
          "file_line": "crates/spark-model/src/weight_map/nvfp4_detect.rs:234-244",
          "detail": "Under exl3_native_enabled(), any prefix still held as packed trellis that reaches quantized_any bails with an explicit error naming the missing dispatch route. Once experts join the native set, load_moe_qwen35's routed-expert and shared-expert quantized_any calls (weight_map/ssm_qwen35.rs:232-274) MUST be branched around, or load fails loudly (fail-closed, no silent double-quant)."
        },
        {
          "name": "skip_routed_experts precedent (loader handoff)",
          "file_line": "crates/spark-model/src/weight_map/ssm_qwen35.rs:87-99,268-269; caller weight_loader/qwen4_exp/ffn.rs:40-51 (passes false today); FP8 sibling ssm_qwen35.rs:307-333",
          "detail": "load_moe_qwen35(skip_routed_experts=true) fills experts with ExpertWeight::null() 'when native ... MoE dispatch handles them' — the exact pattern for EXL3-native experts: keep MoeWeights construction stable, load a parallel Vec of Exl3 expert triples (QuantExpertWeight with QuantWeight::Exl3 arm already exists, weight_map/expert.rs:131-138,190-196) via Exl3Weight::from_store per projection."
        },
        {
          "name": "EP load-skip covers EXL3 suffixes",
          "file_line": "crates/spark-runtime/src/weights.rs:398-433 (should_skip_tensor + parse_expert_index); pinned by test crates/spark-runtime/src/weights/exl3.rs:350-363",
          "detail": "Remote `*.experts.{E}.*` tensors are dropped at load by expert-index parse on the `experts` path segment; .trellis/.suh/.svh/.mul1 parse identically to .weight, and shared_expert never filters. Confirmed: a rank never uploads remote experts' trellis. is_local_expert: crates/atlas-core/src/config/methods.rs:144-147 (contiguous range, last rank takes the remainder)."
        },
        {
          "name": "teardown ordering + sweep backstop",
          "file_line": "crates/spark-model/src/model/types.rs:594-651 (release_pools; weights released LAST at 631-633, sweep at 638); store release crates/spark-runtime/src/weights.rs:476-492; sweep impl crates/spark-runtime/src/cuda_backend.rs:223-265; store adoption factory/build.rs:945",
          "detail": "Kept-packed trellis/suh/svh/mul1 stay in the WeightStore (materializer only removes what it rewrites, exl3_materialize.rs:399-403) — freed by store.release after every layer that reads them is gone. Ptr tables, locks, and scratch owned by layer structs are today reclaimed by gpu.sweep_unreleased with a warning (documented pattern for the NVFP4 MoE ptr tables too); each free removes the ledger entry first, so the sweep cannot double-free."
        },
        {
          "name": "alloc ledger attribution (no tag_alloc_owner on this branch)",
          "file_line": "crates/spark-runtime/src/cuda_backend/alloc_ledger.rs:26-135; #[track_caller] on GpuBackend::alloc crates/spark-runtime/src/gpu.rs:90-96",
          "detail": "This worktree has the call-site-keyed ledger only (grep for tag_alloc_owner: zero hits) — the MEMORY.md owner-tag API (#736) is not merged here. Ledger honesty = allocate each buffer class (ptr tables, mgemm scratch, MoE locks) from its own named helper so alloc_report's by-site/by-file rollups attribute them, exactly like exl3_locks_alloc (exl3_matmul.rs:67-71) already does."
        },
        {
          "name": "cooperative-launch capture veto",
          "file_line": "crates/spark-model/src/model/trait_impl/decode_a.rs:263, decode_a2.rs:282, verify_b.rs:235, verify_c.rs:187-190, verify_c2.rs:184-190 (all key on lm_head_exl3.is_some())",
          "detail": "Cooperative launches are not graph-capturable; today's veto keys ONLY on the native lm_head. If native MoE can ever be enabled while the head is not (e.g. lm_head at unsupported K), every veto site must key on 'any native EXL3 component installed', or decode-graph capture will crash mid-serve."
        },
        {
          "name": "Exl3LmHead scratch/keying precedent",
          "file_line": "crates/spark-model/src/model/lm_head_exl3.rs:98-196 (construction: locks + a_f16 + c_f16 + c_f32_single, all-or-nothing alloc with rollback), 340-372 (scratch keyed by destination logits-arena row); arena rows factory/build.rs:823 (sizes().logits / (vocab*2))",
          "detail": "The template for MoE state: allocate once at construction, size from the arena, key concurrent users by disjoint destination rows, own a private locks buffer, never alloc/sync on the hot path."
        }
      ],
      "design": "RECOMMENDED IMPLEMENTATION (file-touch list at end).\n\n1. MATERIALIZER REDESIGN (native experts)\n- Gate: ATLAS_EXL3_NATIVE_MOE=1, effective only with ATLAS_EXL3_NATIVE=1 (error, not silent-ignore, if MOE is set without NATIVE — mirrors the fail-loud house style). Implement as `exl3_native_moe_enabled()` next to exl3_native_enabled() (exl3_materialize.rs:68-70) and extend `exl3_native_serves(prefix)` (exl3_materialize.rs:83-85) to return true for `.mlp.experts.N.{gate,up,down}_proj` and `.mlp.shared_expert.{gate,up,down}_proj` when the MoE gate is on (reuse the wants_nvfp4_triplet predicate at line 260-262, but EXCLUDE `mtp.` prefixes — MTP experts must keep materializing). Keeping the decision inside exl3_native_serves preserves the materializer/builder single-source-of-truth invariant (build.rs re-derives it).\n- Support predicate: add `exl3_native_supported_moe(&Exl3Weight)` — K in {2,3,4} (mgemm-only path; the {2,4} GEMV-concurrency restriction at exl3_materialize.rs:93-101 does not apply because experts never route through small-row dense GEMV), cb in {Mcg, Mul1}, dims %128 (qwen4_exp: gate/up 2560->640, down 640->2560, all pass; shape-2 divisibility k%32, n%128 passes for both orientations). ADDITIONALLY enforce per-(layer, projection) uniformity: every local expert must share (k_bits, cb) or the WHOLE layer's experts fall back to materialization (one mgemm launch = one K/cb template).\n- What stays packed per expert: {p}.trellis (u16 [in/16, out/16, 16K]) + {p}.suh (f16 [in]) + {p}.svh (f16 [out]) + {p}.mul1 (4-B flag) x3 projections x local experts x48 layers, plus the shared expert's 3. The keep is exactly the existing `continue` branch (exl3_materialize.rs:327-349): skip the rewrite AND the frees, count stats.kept_native.\n- Loader handoff — DO NOT add an Exl3 arm to quantized_any (its return type is the NVFP4-specific QuantizedWeight). Instead, in weight_loader/qwen4_exp/ffn.rs::build_moe: when the MoE gate is on and the store is EXL3, call load_moe_qwen35 with skip_routed_experts=true (routed experts become ExpertWeight::null(), the exact FP8-native precedent documented at ssm_qwen35.rs:87-88), branch the shared expert the same way, and add a new `load_moe_qwen4exp_exl3(store, lp, num_experts, gpu, config) -> Result<(Vec<QuantExpertWeight>, QuantExpertWeight)>` that builds per-projection Exl3Weight::from_store and wraps them in QuantWeight::Exl3 (arm already exists, expert.rs:131-138). Optimization: read the .mul1 flag ONCE per model (73,728 4-byte D2H readbacks otherwise; the checkpoint uses one codebook). Hand the vec to MoeLayer via a new constructor input or post-construction setter (set_lm_head_exl3 precedent), where MoeLayer builds: (a) three device pointer-tables per projection (b_list/suh_list/svh_list over LOCAL experts, dense, no gaps), (b) index 0 = shared expert optional or a separate single-matrix path — recommend serving the shared expert through plain exl3_gemm/exl3_gemv (it is a dense per-token projection, m = all tokens).\n- Idempotence: unchanged by construction — kept prefixes retain .trellis, so a second materialize_exl3 call re-takes the keep branch (test native_mode_keeps_supported_lm_head_packed pins this shape); the pass stays a no-op for non-EXL3 stores (store_has_exl3 gate, line 281). The quantized_any tripwire (nvfp4_detect.rs:234-244) turns any missed consumer into a hard, named load error instead of a silent NVFP4 fallback.\n- Variant sniffing: on a pure-EXL3 checkpoint with native MoE, no .weight_scale exists → detect_nvfp4_variant returns Bf16Raw with its warning (nvfp4_detect.rs:155-162) — harmless (router/gates load via dense_auto; non-expert linears still materialize to BF16 until dense-native lands), but silence/reword the warning for the native case.\n\n2. MEMORY ACCOUNTING (2.05bpw ≙ K=2; per-projection elements 640x2560 = 1,638,400)\n- Per projection: EXL3 packed = trellis in*out*K/8 = 409,600 B + suh/svh (2560+640)*2 = 6,400 B + mul1 4 B = 416,004 B (2.03 bpw; ~2.05 with alloc granularity). NVFP4 triplet = 819,200 (packed) + 102,400 (scale) + 4 = 921,604 B (4.5 bpw).\n- Routed experts, 48 layers x 3 projections: 512 experts (single node, 73,728 tensors): EXL3 30.67 GB vs NVFP4 67.95 GB (packed-weight component alone 60.4 GB ≈ the quoted ~59 GB) → saves ~37.3 GB. EP=2 (256 local, 36,864 tensors): EXL3 15.34 GB vs NVFP4 33.97 GB. Shared expert (144 tensors): 59.9 MB vs 132.7 MB. suh/svh subtotal is 471.9 MB of the 30.67 GB.\n- Ptr tables: 3 arrays x 8 B x E_local x 3 projections x 48 layers = 1.77 MB (E=512) / 0.88 MB (E=256). Per-launch routing buffers: B_indices i64 + B_weights f16 per slot = 10 B/slot; slots = tokens x top_k(10) → decode C=32: 3.2 KB; 4096-token chunk: 409.6 KB (allocate once at max).\n- mgemm scratch (slot model m=1 per (token,expert) slot): A staging fp16 [S,2560] (activations replicated top_k x; bszm_in=1 broadcast only covers the single-token case), A_had fp16 [S,2560] (allocate SEPARATE from A — the gemm's A_had-aliases-A sanction is not documented for mgemm's per-slot slabs), C fp32 [S,640] x2 (gate,up) + [S,2560] (down, doubles as reduction scratch), inter fp16 [S,640]. Sizing at full 4096-token chunk (S=40,960) ≈ 1.05 GB — instead SLOT-BATCH launches at e.g. S_cap = 5,120 slots (512 tokens x 10, groups kept intact since bszm must be a multiple of num_tokens): A 26.2 + A_had 26.2 + C_gate/up 2x13.1 + C_down 52.4 + inter 6.6 ≈ 138 MB, shared across all 48 layers. Decode/verify scale (C=32, K=3 verify → ≤960 slots) needs ≈ 26 MB of that. Plus one MoE locks buffer 4.2 MB.\n- Ledger placement: no tag_alloc_owner exists on this branch — attribution is by allocating call site (#[track_caller], gpu.rs:95). Put ptr-table builds in one named function in a dedicated file (layers/moe/exl3_tables.rs pattern) and the scratch in the BufferArena (extend sizes.rs, which already computes top_k/moe scratch at buffers/sizes.rs:158-179) so it is INSIDE the util pledge (util-pledge fidelity lesson: pools outside the pledge caused #650). If scratch stays layer/model-owned instead, allocate at construction from a single named site so alloc_report shows one legible row.\n\n3. EP\n- CONFIRMED: the loader skip (weights.rs:398-433) drops remote experts' .trellis/.suh/.svh/.mul1 at load — parse_expert_index parses the `experts.{E}` segment regardless of suffix, pinned by the test at weights/exl3.rs:353-363. Remote experts never reach the store, so Exl3Weight::from_store would fail for them — the loader must only iterate config.is_local_expert(e) (same as today's NVFP4 loop at ssm_qwen35.rs:268).\n- Ptr-table shape under EP: build DENSE tables over the 256 local experts (no NULL entries). Host routing maps global expert id → (id - local_start) for local, -1 for remote, writing per-slot B_indices; slot order groups each token's top-10 contiguously with bszm = tokens x 10 and num_tokens = tokens. The kernel skips negative slots in compute AND — critically — in the weighted reduction (exl3_gemm_kernel.cuh:271: `B_indices[t*stride+j] >= 0`). Do NOT use NULL ptr-table entries with valid indices: compute would skip (B==nullptr guard) but the reduction would sum stale scratch — silent corruption. Do NOT use min_index/max_index filtering: it caps bszm at 128 (decode-only scale; a 4096-token prefill chunk has 40,960 slots), routes through the per-device __device__ globals, and duplicates work the host-side -1 mapping already does. Each rank then produces correct partial sums over local experts and the existing EP all-reduce/combine (null-expert = zero contribution semantics, ExpertWeight::null() precedent expert.rs:85-95) is unchanged.\n\n4. ENV-GATE MATRIX AND SAFE DEFAULTS\n- ATLAS_EXL3_NATIVE (default off): master gate; today = lm_head only. Off → EXL3 checkpoints fully materialize (experts→NVFP4 triplets, rest→BF16), byte-identical to current shipping behavior; non-EXL3 checkpoints untouched either way (store_has_exl3 no-op).\n- ATLAS_EXL3_NATIVE_MOE (default off, requires NATIVE=1): routed + shared experts kept packed, mgemm/gemm serving. Off → experts materialize to NVFP4 exactly as today: same quantize_to_nvfp4 kernels, same one-tensor-at-a-time transient order, byte-identical NVFP4 bits and identical peak-memory profile.\n- ATLAS_EXL3_NATIVE_DENSE (future, default off, requires NATIVE=1): GDN in_proj_qkv [2560->10240], in_proj_z [2560->6144], out_proj [6144->2560] and attention q/k/v/o — all %128. Constraint carried from exl3_materialize.rs:93-101: small-row projections run concurrently under co-dispatched prefill finalize, so dense-native either keeps K in {2,4} (GEMV covers all rows<=8, GEMV never touches locks) or grows per-stream locks buffers.\n- ATLAS_EXL3_GEMV_OCC (existing tuning knob, exl3_matmul.rs:239-248).\n- Existing hard incompatibilities to preserve/extend: --dflash and --lora-adapter refuse ATLAS_EXL3_NATIVE (build.rs:242-253); decode-graph capture vetoed (5 sites, all currently keyed on lm_head_exl3.is_some() — re-key on any-native-component if MoE can enable independently). MTP: exclude mtp.* from the native set (MTP experts keep the NVFP4 triplet path via wants_nvfp4_triplet).\n- Byte-identical requirement when gates are off: no exl3_matmul kernel is launched, no locks/scratch allocated, QuantWeight::Exl3 never constructed, materializer output bit-for-bit equal (the keep-vs-rewrite branch is exclusive; nothing else in the pass reads the gate).\n\n5. TEARDOWN / OWNERSHIP\n- Kept-packed expert tensors: remain WeightStore entries; build.rs:945 model.adopt_weight_store(store) hands ownership to the model; release_pools frees weights LAST (types.rs:629-633), after layers (which hold Copy DevicePtrs into them and must free nothing) — same lifetime story as the native lm_head trellis today. Layers/MoeLayer MUST never free Exl3Weight pointers.\n- Ptr tables + locks + scratch: if layer/model-owned, they are reclaimed by gpu.sweep_unreleased at types.rs:638 (the documented backstop the NVFP4 MoE ptr tables and fused weights already use) — functional but logs the 'no ModelResource released' warning every teardown; prefer either (a) putting scratch in the BufferArena (released at types.rs:628) and (b) giving MoeLayer's EXL3 state an explicit release folded into a ModelResource (DerivedWeights precedent, layers/ops/derived_weights.rs:170), keeping the sweep at zero for clean teardowns.\n- Double-count / double-free hazards during the transition: (i) keep-AND-materialize must stay impossible — the materializer branch is `continue` XOR rewrite+free; any new fallback that materializes an expert after partially keeping its siblings must remove+free the kept tensors of that layer first, or residency double-counts; (ii) follow the exl3_materialize pattern (store.remove THEN gpu.free, exl3_materialize.rs:399-403), never the fused-MoE pattern at ssm_qwen35.rs:284-291 which frees a pointer still held by the store (store.release later re-frees it — latent double-free); (iii) mixed mode (MOE off, NATIVE on) is today's tested state and stays the safe intermediate; (iv) the sweep cannot double-free anything a ModelResource freed (forget_alloc removes ledger entries first, cuda_backend.rs:219-222 doc).\n- Abandoned loads: AtlasCudaBackend::Drop sweeps everything (cuda_backend.rs:290-308), covering a load that fails between keep-packed and model construction.\n\n6. CONCURRENCY\n- Serialization today: the model runs on ONE scheduler thread (unsafe Send/Sync contract, types.rs:569-580); decode steps run one at a time on the primary stream (also stated in lm_head_exl3.rs:49-54), and co-dispatched prefill runs the token-parallel GEMM phases STACKED on a single stream (M = Σ req_len; qwen3_ssm/trait_prefill_phase1.rs:200-212), with per-request streams used only for state-carrying ops (conv1d, attention) and the lm_head finalize. MoE FFN GEMMs live in the stacked phase → all exl3_mgemm launches are same-stream, in-order → ONE MoE locks buffer per model, shared across all 48 layers, satisfies the one-concurrent-cooperative-GEMM-per-locks rule with no serialization cost.\n- Cross-subsystem overlap: Exl3LmHead owns its own locks (lm_head_exl3.rs:154), so a co-dispatched finalize head projection (GEMV — locks-free — or GEMM on its own locks) can overlap a MoE mgemm without a locks race. Two concurrent full-GPU cooperative grids on different streams timeslice/serialize at SM level; cooperative co-residency is a per-launch requirement, not a cross-launch deadlock risk.\n- Filtered-mgemm globals: v_indices/v_weights/bszm_sync are module-scope __device__ per CUmodule — i.e. per AtlasRegistry per model instance per device (cuda_backend.rs:126-135: registry is per-model, not process-global). Verified deployment shape: one process per device (EP=2 is one rank/process per node; hot-swap tears the old model down before the new serves), and all mgemm launches are same-stream serialized — so the constraint is satisfied trivially TODAY. It only bites if (a) filtered mgemm ever moves onto co-dispatched streams, or (b) two models in one process serve concurrently. RECOMMENDATION: never use min_index/max_index in Atlas (the -1 mapping subsumes it), making the constraint moot and documenting it as such in the wrapper.\n- Rule to write into the MoE dispatch site docs (mirroring lm_head_exl3.rs:47-54): 'locks is per-subsystem, launches on the primary stream only; a new concurrent caller gets a new locks buffer'.\n\nFILE-TOUCH LIST\n- crates/spark-model/src/weight_map/exl3_materialize.rs — exl3_native_moe_enabled(), extend exl3_native_serves(), add exl3_native_supported_moe(), keep-branch uses the MoE predicate for expert prefixes; tests for keep/idempotence/mixed-K fallback.\n- crates/spark-model/src/weight_loader/qwen4_exp/ffn.rs — branch: skip_routed_experts=true + shared-expert branch + call new EXL3 expert loader; refuse ATLAS_HOLO_MOE_GROUPED_CUTLASS combination.\n- crates/spark-model/src/weight_map/ssm_qwen35.rs (or a new weight_map file) — load_moe_qwen4exp_exl3(): per-projection Exl3Weight::from_store over local experts + shared expert, one-time mul1 read, uniform-(K,cb) validation.\n- crates/spark-model/src/layers/moe/ (new exl3_tables.rs + mod.rs field + dispatch site) — ptr-table build (b/suh/svh lists), MoE locks alloc, slot-batched mgemm decode/prefill path (B_indices -1 mapping, num_tokens grouping, fp32 C + exl3_f32_to_bf16 egress), shared-expert exl3_gemm/gemv path; explicit release.\n- crates/spark-runtime/src/buffers/sizes.rs (+ accessors) — mgemm slot scratch (A, A_had, C, inter, routing buffers) sized from max_batch_tokens x top_k with the slot-batch cap.\n- crates/spark-model/src/factory/build.rs — MOE-gate wiring, veto re-keying (with decode_a.rs/decode_a2.rs/verify_b.rs/verify_c.rs/verify_c2.rs), load-time kernel-presence probe (exl3_mgemm_k{K}_cb{cb}_sh2_f32).\n- crates/spark-model/src/weight_map/nvfp4_detect.rs — soften the Bf16Raw warning for native-EXL3 stores.",
      "risks": [
        "NULL ptr-table entries under weighted reduction are silently WRONG: the reduction epilogue skips only negative B_indices, not null B_list entries (exl3_gemm_kernel.cuh:271) — a 512-wide table with NULLs for remote experts sums stale fp32 scratch into token outputs. Remote experts must be -1 indices over a dense local table; add a parity test that routes to a remote expert and asserts zero contribution.",
        "Mixed K/cb across experts breaks the single-template mgemm launch: EXL3 exports can vary bitrate per tensor. Without a per-(layer,projection) uniformity check the loader would build a table the kernel decodes at the wrong K — validate at load and fall back to materialization for non-uniform layers (a fallback that then double-holds memory if the layer's siblings were already kept — make keep/materialize atomic per layer).",
        "mgemm scratch at full prefill-chunk scale is ~1.05 GB (40,960 slots); it must be slot-batched (~138 MB at 5,120 slots) AND live inside the util pledge/BufferArena — allocations outside the pledge are the documented DFlash-OOM/util-pledge failure class on GB10, where over-allocation hangs the box.",
        "A_had-aliases-A is upstream-sanctioned only for the single-matrix gemm; for mgemm's per-slot slabs it is unverified — allocate a separate A_had (cheap at slot-batch scale) or prove aliasing safety against the vendored kernel before saving the 26 MB.",
        "Decode-graph veto currently keys on lm_head_exl3.is_some() at 5 sites; if native MoE can enable while the head falls back (e.g. head K outside {2,4}), cooperative mgemm launches under capture fail with CUDA_ERROR_STREAM_CAPTURE_UNSUPPORTED mid-serve — re-key the veto on any native component.",
        "The 128-slot cap plus module-scope __device__ globals make the filtered-mgemm path unusable for prefill and a latent one-per-device serialization constraint — enforce 'never pass min_index>=0 from Atlas' in the wrapper (debug assert), not just by convention.",
        "Teardown sweep noise: layer-owned ptr tables/locks/scratch will trip the 'no ModelResource released' warning every clean teardown (types.rs:638-645) unless given an explicit owner; operators treat that warning as a leak signal, so leaving native-MoE state to the sweep erodes the ledger's honesty.",
        "Load-time mul1 readback: 73,728 synchronous 4-byte D2H copies (one per Exl3Weight::from_store) will measurably slow load — read the codebook flag once per model.",
        "Cooperative-launch co-residency: mgemm grids are sized from sm_count with the upstream heuristic (mgemm.rs:96-106); an occupancy regression (e.g. future smem/register changes) makes cudaLaunchCooperativeKernel FAIL cleanly but fatally at serve time — keep the load-time kernel probe and a smoke launch in the gate harness.",
        "Small-M MoE cost cliff: grouped/persistent kernels cost the same at 1 row as at many (small-M MoE prefill trap) — decode-path throughput of mgemm at 10-320 slots must be A/B'd against the shipped fused NVFP4 decode kernels before making native experts the default; the memory win is real but the decode perf is unproven.",
        "quantized_any's Bf16Raw fallback warning will fire on pure-EXL3 checkpoints in native-MoE mode ('quality will be inferior') — cosmetic but misleading; reword or gate it.",
        "EP=2 correctness depends on the existing all-reduce treating skipped slots as exact zeros; the -1-slot reduction gives true zero contribution, but verify the combine path does not normalize by local slot count anywhere (the CUTLASS-EP #781 bug class: group filters testing rows but not weights)."
      ]
    }
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Read"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "read:upstream-moe",
      "phaseIndex": 1,
      "phaseTitle": "Read",
      "agentId": "aa4959c4c31a420e5",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1788283487361,
      "queuedAt": 1788283487343,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Upstream ExLlamaV3 serves EXL3 MoE through THREE distinct t…",
      "promptPreview": "Context: Atlas (Rust + CUDA inference engine, GB10/sm_121a only, BF16 activations) has a\nparity-proven native EXL3 trellis matmul layer on branch wip/exl3-research: kernels/gb10/common/exl3_matmul.cu\n(fused suh/H128 -> trellis decode -> MMA -> H128/svh; gemv M<=8 K{2,3,4}; gemm all K shapes 1-4; mgemm =\npointer-table multi-matrix MoE form with B_indices routing, B_weights per-expert output weights…",
      "lastProgressAt": 1788283835771,
      "tokens": 132277,
      "toolCalls": 25,
      "durationMs": 348409,
      "resultPreview": "{\"summary\":\"Upstream ExLlamaV3 serves EXL3 MoE through THREE distinct tiers keyed on token count T (rows after flattening bsz*q_len), never one mechanism: (1) T<=MAX_BSZN=8 -> BC_BlockSparseMLP.run_bszN, a per-T CUDA graph doing exactly three exl3_mgemm calls (gate, up, down) over bszm = T*top_k slots (qwen4_exp: 10T<=80 slots), A gathered so slot s=t*top_k+j holds a copy of token t's row, indices…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "read:atlas-moe",
      "phaseIndex": 1,
      "phaseTitle": "Read",
      "agentId": "a4a72c31a56fd2510",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1788283487361,
      "queuedAt": 1788283487343,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Atlas serves qwen4_exp's 512 routed experts through four de…",
      "promptPreview": "Context: Atlas (Rust + CUDA inference engine, GB10/sm_121a only, BF16 activations) has a\nparity-proven native EXL3 trellis matmul layer on branch wip/exl3-research: kernels/gb10/common/exl3_matmul.cu\n(fused suh/H128 -> trellis decode -> MMA -> H128/svh; gemv M<=8 K{2,3,4}; gemm all K shapes 1-4; mgemm =\npointer-table multi-matrix MoE form with B_indices routing, B_weights per-expert output weights…",
      "lastProgressAt": 1788283906507,
      "tokens": 192213,
      "toolCalls": 48,
      "durationMs": 417782,
      "resultPreview": "{\"summary\":\"Atlas serves qwen4_exp's 512 routed experts through four decode surfaces (C=1 fused GEMV, K=2/K=3 fused batch, token-major N>=4, per-token batched fallback) and one grouped-GEMM prefill ladder, all driven by device-resident routing state at ctx.buffers.scratch() (u32 indices + f32 probs) and per-expert pointer tables (ExpertPtrTable: [E] u64 packed_ptrs / [E] u64 scale_ptrs / [E] f32 s…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "read:gdn-attn-routing",
      "phaseIndex": 1,
      "phaseTitle": "Read",
      "agentId": "ab0531956985b60ef",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1788283487361,
      "queuedAt": 1788283487343,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Serving qwen4_exp's GDN + attention dense linears natively …",
      "promptPreview": "Context: Atlas (Rust + CUDA inference engine, GB10/sm_121a only, BF16 activations) has a\nparity-proven native EXL3 trellis matmul layer on branch wip/exl3-research: kernels/gb10/common/exl3_matmul.cu\n(fused suh/H128 -> trellis decode -> MMA -> H128/svh; gemv M<=8 K{2,3,4}; gemm all K shapes 1-4; mgemm =\npointer-table multi-matrix MoE form with B_indices routing, B_weights per-expert output weights…",
      "lastProgressAt": 1788284009682,
      "tokens": 178065,
      "toolCalls": 55,
      "durationMs": 520956,
      "resultPreview": "{\"summary\":\"Serving qwen4_exp's GDN + attention dense linears natively from EXL3 trellis is feasible with NO kernel changes: every consumer either already runs per-projection GEMM/GEMVs into separate/offset output buffers (attention q/k/v/o) or is parameterized by an input row-stride that lets the fused-QKVZ concat be replaced by two packed weights writing a split arena (GDN). Upstream exllamav3 s…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "read:scope-accounting",
      "phaseIndex": 1,
      "phaseTitle": "Read",
      "agentId": "a56c018a3a84227d1",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1788283487361,
      "queuedAt": 1788283487344,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Cross-cutting review for serving qwen4_exp's 512 routed exp…",
      "promptPreview": "Context: Atlas (Rust + CUDA inference engine, GB10/sm_121a only, BF16 activations) has a\nparity-proven native EXL3 trellis matmul layer on branch wip/exl3-research: kernels/gb10/common/exl3_matmul.cu\n(fused suh/H128 -> trellis decode -> MMA -> H128/svh; gemv M<=8 K{2,3,4}; gemm all K shapes 1-4; mgemm =\npointer-table multi-matrix MoE form with B_indices routing, B_weights per-expert output weights…",
      "lastProgressAt": 1788284081387,
      "tokens": 171397,
      "toolCalls": 54,
      "durationMs": 592661,
      "resultPreview": "{\"summary\":\"Cross-cutting review for serving qwen4_exp's 512 routed experts (+shared expert) and later the GDN/attention dense linears directly from packed EXL3 trellis on branch wip/exl3-research. Key findings: (1) the existing gate machinery (exl3_native_serves/exl3_native_supported as the single source of truth shared by materializer and builder) extends cleanly to an ATLAS_EXL3_NATIVE_MOE=1 la…"
    }
  ],
  "totalTokens": 673952,
  "totalToolCalls": 182
}