name: TUI threading contract

# The dashboard's render loop runs on its own OS thread and must never poll a
# future. Its only interaction with async work is `try_recv` on a channel.
#
# That rule was previously stated in prose in two module docs — and they
# contradicted each other: `recipe/fetch.rs` said "no spawn_blocking, no
# block_on, no future in this file", while `tui/chat.rs` and
# `atlas-plugin/src/executor.rs` legitimately DO spawn tokio tasks and hand
# results back over a std channel. Prose that disagrees with itself is not a
# contract. This is the version a machine can check.
#
# What is forbidden is not async — it is BLOCKING THE RENDER THREAD ON IT.
# `block_on` and `block_in_place` are the two ways to do that by accident.

# Least privilege: this job only reads the checkout to grep it. Without an
# explicit block the GITHUB_TOKEN inherits the repository default, which
# CodeQL flags (actions/missing-workflow-permissions) — and it was right to,
# since nothing here writes anything. Matches ci.yml.
permissions:
  contents: read

# Only the newest commit on a ref is worth checking; superseded runs otherwise
# sit in the queue holding runner slots that the newest commit is waiting for.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  # Manually runnable. During the 2026-08-06 Actions incident, webhook triggers
  # were throttled to aid recovery, so pushes stopped creating runs at all --
  # and with no dispatch trigger there was no way to ask for one. A check that
  # can only be started by an event you do not control is a check you cannot
  # get a verdict from when you most need it.
  workflow_dispatch:
  # No path filter on pull_request: this check joins the required set, and a
  # path-filtered required check never reports on PRs outside its paths,
  # leaving them permanently "expected" and unmergeable. The job is one grep
  # over two directories — running it on every PR costs seconds.
  pull_request:
  push:
    branches: [main]
    paths:
      - 'crates/spark-server/src/tui/**'
      - 'crates/spark-server/src/recipe/**'
  # Merge queue: queue commits fire none of the events above, and this check
  # is required — without this trigger the entry hangs until timeout.
  # Unfiltered for the same reason as pull_request.
  merge_group:

jobs:
  no-blocking-on-the-render-thread:
    runs-on: >-
      ${{ (github.event_name != 'pull_request'
           || github.event.pull_request.head.repo.full_name == github.repository)
          && (vars.PR_CHEAP_RUNNER || 'ubuntu-latest')
          || 'ubuntu-latest' }}
    name: No block_on under tui/ or recipe/
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Check the render thread never blocks on a future
        shell: bash
        run: |
          set -euo pipefail
          # Only real call syntax counts -- comments explaining the rule are
          # allowed to name it.
          #
          # ★ TEST FILES ARE EXCLUDED, and the distinction is the whole point of
          # the rule rather than a loophole in it. What is forbidden is BLOCKING
          # THE RENDER THREAD on a future: that freezes the dashboard and, since
          # the TUI is the server's foreground, hides the server with it. A test
          # that stands up its own runtime and drives an async fn to completion
          # is not the render thread and cannot freeze anything -- it is the
          # ordinary way to test async code, and forbidding it would push the
          # tests toward worse shapes (sleep-and-poll) for no safety gained.
          #
          # The exclusion is by FILENAME (`*_tests.rs`), which is the repo's
          # convention for `#[cfg(test)]` siblings mounted with `#[path]`. If
          # production code is ever put in a file named that way, this check
          # will not see it -- that is the cost, and it is smaller than the
          # alternative.
          #
          # grep's exit 2 means the SCAN FAILED -- a renamed or deleted
          # directory, an unreadable file -- and GNU grep returns 2 even when it
          # also found matches. `if hits=$(...)` read 2 as "no hits" and fell
          # straight through to the OK line, so this REQUIRED check reported a
          # clean tree for a scan that never happened. The two directory names
          # are hard-coded again in this workflow's push `paths:` filter, so
          # renaming one is precisely the change that would have tripped it.
          # Stderr no longer goes to /dev/null either: the reason belongs in the
          # log of the job that refuses.
          rc=0
          hits=$(grep -rnE '\.(block_on|block_in_place)\(' \
                   --include='*.rs' --exclude='*_tests.rs' \
                   crates/spark-server/src/tui/ \
                   crates/spark-server/src/recipe/) || rc=$?
          if [ "$rc" -ge 2 ]; then
            echo "::error::The block_on scan did not run (grep exited $rc). This check cannot vouch for a tree it failed to read -- most likely one of the two directories above was renamed or removed. Update this workflow, including its push paths: filter."
            exit 1
          fi
          if [ "$rc" -eq 0 ]; then
            echo "::error::The TUI render thread must never poll a future."
            echo "$hits"
            echo
            echo "The render loop's only interaction with async work is try_recv"
            echo "on a channel (see tui/chat.rs and tui/bench_preflight.rs for"
            echo "the sanctioned shape: spawn on the runtime, answer over an"
            echo "mpsc the tick drains). Blocking the render thread on a future"
            echo "freezes the dashboard and, because the TUI is the server's"
            echo "foreground, hides the server with it."
            exit 1
          fi
          echo "OK: no block_on/block_in_place under tui/ or recipe/"
