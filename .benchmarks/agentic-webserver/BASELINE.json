{
  "schema": 2,
  "hardware": {
    "gb10": {
      "default": "Qwen/Qwen3.6-35B-A3B-FP8",
      "models": {
        "Qwen/Qwen3.6-35B-A3B-FP8": {
          "recipe": "qwen3.6/qwen3.6-35b-a3b-fp8-bf16head",
          "note": "\u2605 SETTLED BY EXPERIMENT: the 10/10 threshold belongs to a DIFFERENT HARNESS, and this built-in benchmark is not equivalent to it. Left FAILING on purpose. HEAD-TO-HEAD, 2026-08-06, same box, same binary (0cdd2d5f), same serve (the benchmark-pr golden flags: --ssm-cache-slots 256 --ssm-checkpoint-interval 16 --max-batch-size 2 --gpu-memory-utilization 0.70): the SHELL harness (bench/fp8_dgx2_drift/harness/run_tier.sh, N=10) scored webserver_ok 10/10, cargo_valid 10/10, mean wall 133 s. THIS built-in plugin benchmark, five tiers of N=10, scored webserver_ok 3/9/6/7 (recipe) and 6 (golden flags), followed_directions 0/6/4/7 and 5, mean wall 40-120 s per run. SO: the model has NOT regressed -- the shell harness reproduces 10/10 on today's binary. Serve config is REFUTED as the cause (tested directly, same spread). The two harnesses simply measure different things; the built-in one gives the agent materially less room (133 s/run vs 40-120 s) and scores a different rubric. This is the migration debt the repo already records as owed: the benchmark-pr gates are hand-driven, and porting them to the built-in suite is unfinished. Importing the shell harness's numbers into the built-in benchmark's baseline was my error -- a threshold does not transfer between instruments, exactly like a BFCL score across draws. TO FIX, one of: (a) bring the built-in agentic benchmark to parity with run_tier.sh and re-verify it reproduces 10/10, then keep these bounds; or (b) establish a baseline measured ON this benchmark, stated as a distribution over several tiers rather than a point. DO NOT simply lower the counts to whatever today's run produced. || original: Gate A (benchmark-pr skill): N=10 agentic-webserver on the 35B MoE flagship \u2014 10/10 webserver_ok, 10/10 followed_directions, \u03a3wall <= 1300 s. The two 10/10 counts are ABSOLUTE correctness bounds and take no noise. \u2605 \u03a3wall is DELIBERATELY NOT RATCHETED to the 978.13 s best (PR #388, dgx3): unlike the BFCL scores, tier \u03a3wall carries +/-200-300 s of run-to-run noise from single 230-320 s outlier iterations, so a ~978 s ceiling would fail constantly on healthy builds. 1300 s is a BLOWUP DETECTOR, not a perf gate \u2014 it cannot see a 5-10% TTFT regression, which is what the two TTFT gates are for. Historical band \u03a3wall 950-1287 s; high-water (fastest) 978.13 s recorded here for reference only. Walls are box-specific: compare like-for-like or not at all.",
          "metrics": {
            "webserver_ok": {
              "min": 10.0
            },
            "followed_directions": {
              "min": 10.0
            },
            "sum_wall_s": {
              "max": 1300.0
            }
          }
        }
      }
    }
  }
}
